struct a {
  int *b
};
long c;
*d;
e;
f(struct a *i) {
  int g, h;
  while (d[h] != 65535 && d[h] != e)
    h++;
  if (d[h] == 65535)
    j();
  h = 0;
  for (; h < 3; h++) {
    g = k(c - h);
    i->b[h * 2] = i->b[h] = g >> 8;
  }
}
