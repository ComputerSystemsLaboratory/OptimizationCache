struct a {
  int b;
  int c;
  struct d *e;
  int *f
};
struct d g;
struct d {
  int cqn
} h() {
  struct a *j = k();
  int l, m, i;
  for (; i < j->b; i++) {
    g = j->e[i];
    ++l;
  }
  i = 0;
  for (; i < j->c; i++) {
    j->f[i] = i;
    ++m;
  }
  while (m--)
    n(j[m]);
  while (l--)
    o(j[l]);
}
