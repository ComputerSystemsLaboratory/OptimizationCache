struct a {
  long b;
  long c
};
d, e, f;
g(struct a *j) {
  int h, i, a, b, c;
  i = c = f;
  b = k();
  if (c ? l() : m()) {
    n(e);
    i = 1;
  }
  if (c ? o() : b ? 0 : p())
    a = 0;
  else
    a = 1;
  if (a || j->b)
    if (c ? j->c : b ? q(e, j->b, j->c) : r(e, j->b, j->c))
      if (d)
        i = 1;
  if (a || h) {
    s();
    if (a || t())
      return i;
  }
}
