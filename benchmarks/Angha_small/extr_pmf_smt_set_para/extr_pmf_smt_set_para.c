struct a {
  int b
};
struct c {
  int d
};
e, f;
g(struct a *h) {
  struct c *i;
  if (!i)
    return h->b & 40 ? f : e;
  switch (i->d) {
  case 3:
  case 2:
    break;
  default:
    return e;
  }
}
