a, b;
c() {
  int d;
  char e;
  char *f;
  if (a) {
    asprintf(&f);
    if (f)
      e = g();
  }
  if (e)
    d = strtoul(e, f, 0);
  if (b && d)
    errx();
  else if (b)
    d++;
  h();
  if (d)
    while (d <= c)
      ;
}
