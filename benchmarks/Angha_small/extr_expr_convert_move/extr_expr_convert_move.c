enum a { b } c;
d, e, f, g, h, i, j, k, n;
*l, *m;
void o(p, q, s) {
  t();
  int r = s < 0 ? h : s ? j : g;
  if (d) {
    k = 0;
    o();
    return;
  }
  if (r) {
    if (u())
      d = v() ? m : l;
    n = m;
    w(l ? e : f);
    o(p);
    return;
  }
  if (c)
    w(p, q);
  else {
    enum a a = t;
    for (; a != i; a = 0)
      ;
  }
}
