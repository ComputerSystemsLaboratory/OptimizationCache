typedef a;
b, c, f;
*d, *e;
g() {
  a h, aid, l, i, j, k;
  l = m();
  for (; 0 < f;)
    ;
  for (; j < f; j++) {
    i = 0;
    for (; i < e[j]; i++)
      if (n(l + 2 + i) != d[i])
        break;
    if (i == e[j])
      h = j;
    else
      k++;
  }
  if (k)
    aid = o();
  if (aid)
    if (b)
      p(aid + 1);
    else
      p(c, aid + 1, h);
}
