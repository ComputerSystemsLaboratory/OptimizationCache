a, b, c, d, e, f, g, h, i, j;
k() {
  int l = 0;
  switch (h)
  case 0:
    if (d && m())
      return 0;
  if (d)
    if (f)
      if (a && n())
        ;
      else
        i == a &&n();
  if (d && o())
    ;
  else if (0 == d && c)
    if (a)
      ;
    else if (i == d && d)
      ;
    else if (i == d && 0 == d && o())
      ;
    else if (d == c)
      p();
  if (f && q())
    return g;
  if (e || 0 == b || 0 == f)
    if (j || l)
      r();
}
