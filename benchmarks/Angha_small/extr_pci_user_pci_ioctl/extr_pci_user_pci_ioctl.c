struct a {
  char *b
};
struct c {
  int d
};
struct g {
  struct a conf;
  struct c cfg
};
struct e {
  long f
};
h;
i;
j;
k;
l() {
  struct e *cio;
  struct g *dinfo;
  int m;
  switch (k)
  case 5:
    cio = malloc;
  if (cio)
    if (cio > 0)
      cio = h;
  dinfo = n();
  for (; dinfo; dinfo = j++) {
    i = 0;
    if (dinfo->cfg.d)
      o();
    if (i)
      p(dinfo->conf);
    else
      dinfo->conf.b[0] = '\0';
    if (m || q(&dinfo->conf))
      break;
  }
  cio->f = j;
}
