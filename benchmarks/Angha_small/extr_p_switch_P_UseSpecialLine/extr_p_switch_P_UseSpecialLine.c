struct a {
  int b;
  int c
};
d, e;
f(struct a *g) {
  if (e)
    switch (g->b)
    case 4:
      return 0;
  if (d) {
    if (g->c)
      return 0;
    switch (g->b)
    case 33:
    case 4:
      return 0;
  }
  switch (g->b)
  case 17:
  case 8:
    h();
}
