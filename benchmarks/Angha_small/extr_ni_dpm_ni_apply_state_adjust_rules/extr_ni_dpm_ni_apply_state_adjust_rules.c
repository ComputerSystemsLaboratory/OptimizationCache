struct {
  long a
} * b;
struct c {
  int d;
  struct e *f
};
struct e {
  long g;
  long h;
  long j;
  long k;
  int flags
};
l;
m() {
  struct c *n = o();
  int *p;
  int i;
  p = b;
  if (b)
    for (; n->d;) {
      if (n->f > p)
        n->f[0].g = p;
      if (n->f > p)
        n->f[0].h = p;
      if (n->f > p)
        n->f[0].j = p;
      n->f[0].k = p;
    }
  for (; 1 < n->d;) {
    if (n)
      n->f[1].h = n;
    for (; i < n->d;)
      ;
    for (; n;)
      for (; n->d; i++) {
        if (n->f)
          n->f[1].g = n;
        n->f[1].k = n->f;
      }
  }
  for (; n->d;)
    q();
  r();
  for (; n->d;)
    for (; 0 < n->d;)
      if (n)
        if (b->a)
          n->f[0].flags = l;
}
