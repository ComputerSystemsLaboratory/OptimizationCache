a, b;
*d;
f() {
  int c;
  if (*d)
    while (*d) {
      char *e = d;
      if (h() || *e)
        break;
      for (; *e != ';'; ++e)
        ;
    }
  switch (c) {
  case 'x': {
    char *g = i(d);
    for (; *g;)
      if (*g == ':' && b)
        break;
  }
  case 'S':
    a;
  }
}
