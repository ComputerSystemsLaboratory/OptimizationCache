struct b {
  int c;
  int d
};
e, g, a;
*f, *h;
m() {
  int b, c, i, j, k = 0, l = n() - 1;
  struct b *d;
  for (; a;) {
    d = h;
    if (d->c) {
      if (d)
        k = l;
      break;
    }
    c = d;
    j = 0;
    for (; j < c; j++)
      o(j >= d == j < d->d);
    if (j)
      b = 0;
    for (; i < g; i++) {
      n(i);
      p(f[i]);
      if (e)
        if (q())
          continue;
      b = 1;
      break;
    }
    if (b) {
      if (e)
        r();
    } else
      r();
  }
  if (k == l)
    return g;
  if (a)
    n();
}
