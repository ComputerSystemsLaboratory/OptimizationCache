struct d {
  long e;
  int f;
  struct b *g;
  int h;
  int i;
  int j;
  int k;
  int l;
  int m;
  int n;
  int o;
  int p;
  int q;
  int r;
  int s;
  int t;
  int u;
  int v;
  int w;
  struct d *x;
  long y;
  int aa;
  long ab;
  int ac;
  long ad;
  int ae;
  int af
} * c;
struct b {
  void *ag;
  struct d *ah
} * d;
a, b, e, f;
void *z();
struct b *ai() {
  struct d *g = z();
  d->ag = a;
  g->j = g->af;
  g->ae = g->e = g->ad = g->ac;
  g->ab = g->f;
  g->aa = g->y;
  for (; g->e;)
    c[f].x = g;
  aj(g->w);
  aj(g->v);
  aj(g->u);
  aj(g->t);
  ak(g->s);
  al(g->h);
  g->r = d->ah = g;
  am();
  d->ah = g;
  if (b)
    g->p = an(g, g->q);
  if (e) {
    ao(g->k);
    ap(g->p);
    aq(g->o);
    ar(g->n);
    ar(g->m);
    as(g->l);
    aq(g->g);
  }
  g->i = 0;
  return g->g;
}
