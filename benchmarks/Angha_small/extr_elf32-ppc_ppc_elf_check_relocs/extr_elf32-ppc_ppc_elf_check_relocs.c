struct a {
  int aa;
  int b
};
struct {
  int ab;
  int ac
} * c;
d, a;
e() {
  for (;;) {
    int f;
    if (f)
      h();
    switch (a) {
    case 1:
      if (c && i())
        return d;
      if (c[1].ac && i())
        return d;
      c[0].ab &&j();
      struct a *g;
      if (g->b) {
        g = k();
        g->aa = 0;
      }
      g->aa++;
    }
  }
}
