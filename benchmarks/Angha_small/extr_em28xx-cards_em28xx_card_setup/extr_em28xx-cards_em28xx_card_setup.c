struct a {
  int b;
  long c;
  long d
};
struct e {
  int f;
  struct a board
};
enum g { h } i;
j, k;
l(struct e *m) {
  m->board.d;
  switch (m->f)
  case 3:
  case 8:
    m->board.b = 1;
  if (m->board.b)
    p();
  if (m->board.c)
    p();
  if (m->board.c == k) {
    int n = m;
    enum g o = n ? j : i;
    q(o);
  }
}
