struct a {
  long *b;
  long c
};
typedef d;
e, f, g, k, l, m, n, s;
long h, j;
v(d w, struct a *x) {
  int q, r, i, a, b, c;
  l = y();
  if (l && (e || f))
    if (r)
      g = w;
  while (w) {
    if (h == e && j & 8)
      ;
    else if ((h == e || f) && j & 60129542144) {
      int t = j >> 6, d = j;
      if (q)
        q = t;
      else if (d == 2)
        ;
      else if (d && t) {
        g = l;
        break;
      }
    } else if (h)
      k = j;
    else if (f)
      z();
    if (h == j) {
      int u = j >> 3, o = j;
      if (o && u)
        s = 1;
      if (o && u < 0)
        n = 1;
    }
    a = b;
    for (; x->c;)
      if (j)
        x->b = a;
    c = 0;
    if (x->b) {
      long p;
      c = p;
    } else if (m)
      c = aa();
    x = c;
    if (c) {
      b = ab();
      for (; i < x; i += 8)
        x->b[8] = a;
    }
  }
  return g;
}
