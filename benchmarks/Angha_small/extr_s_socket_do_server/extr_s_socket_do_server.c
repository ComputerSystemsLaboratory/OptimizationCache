struct a {
  int addr
};
b, c, d;
*e;
void *f();
g() {
  if (b && h() == c)
    ;
  else
    0 == c &&h();
  struct a i;
  char *a, *j;
  int k = 0;
  if ((i.addr = f) && l(&i) && (a = m()) && (j = n()) && o())
    k = 1;
  p();
  q(a);
  q(j);
  r(i);
  if (k) {
    e = f();
    if (e)
      do
        s(e);
      while (d);
  }
}
