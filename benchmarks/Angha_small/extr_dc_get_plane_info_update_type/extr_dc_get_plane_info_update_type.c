struct {
  int b;
  int c;
  int d;
  int e;
  int f;
  int g
} * b;
struct {
  int *h;
  struct i *j
} * a;
struct i {
  long k;
  long l;
  long ab;
  long ad
};
m;
n() {
  if (a->h != a->j->l)
    b->b = 1;
  if (a->h != a->j->ab)
    b = 1;
  if (a->h != a->j)
    b->d = 1;
  if (a->h != a->j->ad)
    b->e = 1;
  if (a->h != a->j)
    b->f = a->h;
  if (a)
    m = a;
  if (a->h != a->j)
    b->g = 1;
  o(a->h, a->j);
}
