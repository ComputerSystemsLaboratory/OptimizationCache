typedef long a;
b, c, d, e, f, g, h, i, j, k, l, m;
n() {
  char o, ab;
  char *ad;
  a ae, af;
  ad = ae = k;
  af = "";
  if (j)
    if (m) {
      af = &ab;
      ae -= af;
      ad += af;
    } else {
      ae -= af;
      ad += af;
    }
  if (i)
    p();
  if (l) {
    af = o;
    if (ae)
      goto ai;
    ae -= af;
    ad += af;
  } else {
    ae -= af;
    ad += af;
  }
  if (h)
    if (g)
      if (m) {
        if (e)
          goto ai;
        ae -= af;
        ad += af;
      } else {
        ae -= af;
        ad += af;
      }
    else if (f)
      if (l) {
        af = &o;
        if (e)
          goto ai;
        ae -= af;
        ad += af;
      } else {
        ae -= af;
        ad += af;
      }
    else {
      ae -= af;
      ad += af;
    }
  if (e)
    q();
  if (e & c)
    q();
  if (e & d)
    q();
  if (e & b)
    q(ad, ae);
ai:;
}
