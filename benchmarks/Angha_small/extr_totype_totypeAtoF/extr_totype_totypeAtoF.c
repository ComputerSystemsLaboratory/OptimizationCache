a, b, c;
double d;
f(*g) {
  h();
  int i = 1, e, j, k = 0;
  if (g >= h)
    return 0;
  if (*g == '-') {
    i = -1;
    g++;
  } else if (*g)
    g++;
  while (l()) {
    c = j++;
    while (g)
      e = g++;
  }
  if (c)
    d = i < 0 ? -(double)0 : 0;
  if (a && e)
    e *= 10;
  e /= i < 0 ? -c : c;
  if (e)
    d = c;
  b = d;
  return g && j && k == 0;
}
