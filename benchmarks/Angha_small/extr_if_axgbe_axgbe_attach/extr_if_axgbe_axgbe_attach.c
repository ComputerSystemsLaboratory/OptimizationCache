struct a {
  int b;
  int c;
  int d;
  int e;
  int f;
  int **g;
  int *h;
  int *i;
  int *k;
  int *l;
  int *m;
  int *n;
  int *o
};
struct p {
  struct a prv
} q() {
  struct p *r;
  int *s[1];
  int *t[4], j;
  r = u();
  if (v()) {
    w(s);
    r->prv.k = s[0];
    r->prv.i = s[1];
    r->prv.o = s[2];
    r->prv.n = s[3];
    r->prv.m = s[4];
    r->prv.h = s[5];
    j = 6;
    for (; 6 < s;)
      r->prv.g[0] = s[j];
  }
  w(t);
  r->prv.o = t[0];
  r->prv.n = t[1];
  r->prv.m = t[2];
  r->prv.l = t[3];
  r->prv.k = s[0];
  r->prv.i = s[1];
  r->prv.h = s[2];
  if (j)
    r->prv.b = r->prv.c = r->prv.d = r->prv.e = 1;
  r->prv.f;
}
