struct a {
  int *b;
  int *c
} * d;
char e;
*f;
g;
h() {
  int i;
  struct a *j = k();
  while (f) {
    f++;
    if (e) {
      if (l())
        d++;
      continue;
    }
    e = f;
    switch (e) {
    case 'l':
      e &&m();
      for (; i < n(); i++)
        g = j->c[i];
      if (i)
        g = j->b[i];
      if (i)
        for (; d;)
        case 'z':
          return 0;
    }
  }
}
