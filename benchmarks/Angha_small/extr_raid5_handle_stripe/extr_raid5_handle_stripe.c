struct a {
  int b;
  int c;
  int d;
  long e;
  long f;
  long g
};
h, i;
j() {
  struct a k;
  if (k.g && l())
    m(&k);
  n(k.d, k.b, k.f, k.e);
  h = i;
  for (; h;)
    i || l();
  if (i || k.e || k.b)
    if (k.c)
      for (; k.c;) {
        l();
        k.d++;
      }
}
