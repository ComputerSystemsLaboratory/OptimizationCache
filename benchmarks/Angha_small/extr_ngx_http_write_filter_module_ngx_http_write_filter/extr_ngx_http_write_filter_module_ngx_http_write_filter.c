struct a {
  int b;
  int d;
  int e;
  long f
};
struct g {
  long h
};
i, j;
k(struct a *l) {
  long h, m, a;
  struct g *c = l;
  if (l)
    l->d = 0;
  if (l->d) {
    if (l->e)
      n();
    a = l->d - c->h;
  } else
    a = j;
  h = c->h;
  o(a);
  m = c->h;
  if (l->f) {
    h -= l->f;
    if (h < 0)
      h = 0;
    if (c->h < 0)
      m = 0;
  }
  i = m - h / l->d;
}
