struct {
  int *a
} * b;
c;
d;
e;
*g;
h() {
  long *dj = e;
  long i = b->a[d];
  long j = b->a;
  int k;
  long l;
  int m;
  while (j && m || c) {
    if ((*dj ? f() : *g) < *dj)
      return 1;
    if (k) {
      if (l)
        if (m == 0)
          l = i;
    } else {
      k;
      if (l)
        k = m--;
    }
    g++;
  }
  if (i == 0 && j == 0)
    return 0;
  if (i)
    return 1;
  if (memcmp < i)
    return 0;
}
