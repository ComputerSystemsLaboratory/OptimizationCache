a, b, d, g, h, l;
*c, *e, *f, *j;
m() {
  for (; l < d; l++)
    ;
  int i = g, k, o = a ?: c;
  if (h) {
    int n = a ?: p(), o = c;
    if (a)
      for (;;)
        e[n] = o;
    for (; k < c[0]; k++) {
      q(i + k);
      f[i + k] = k;
    }
  }
  if (a)
    for (;;)
      e = 0;
  for (; k < c[0]; k++) {
    q(k);
    f[i + k] = o;
    while (b)
      j[b] = b;
  }
}
