struct {
  int b;
  int c;
  int d
} * f;
e, g, h, i, j, k, l, a, m, n;
c() {
  int b;
  while (1) {
    if (f)
      ;
    if (e)
      b = 8;
    else
      switch (l)
      case 8:
        if (f->d)
          b = 0;
    n++;
    switch (b)
    case 8:
      a = k;
    if (n)
      if (abs(h) & 7)
        m = abs(j);
    if (abs(m) > abs(i))
      if (abs(f) > abs(g))
        f = 0;
  }
}
