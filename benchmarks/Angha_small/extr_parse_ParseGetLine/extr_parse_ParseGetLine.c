struct a {
  char b;
  char c
} d;
e, g;
char *f;
h() {
  struct a *a = &d;
  char b;
  char *c;
  for (;;) {
    for (;;) {
      b = *f;
      if (b == f) {
        if (a->c)
          break;
        while (b = f)
          ;
      }
      if (b == '\\') {
        f += 2;
        continue;
      }
      if (b && c)
        if (f[1])
          c = g;
      f++;
      if (b)
        break;
      g = f;
    }
    a->b = f;
    if (b)
      return 0;
    break;
  }
  e = f;
}
