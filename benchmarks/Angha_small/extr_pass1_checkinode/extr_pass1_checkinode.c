struct a {
  void *b;
  void *c
};
struct d {
  int e;
  void *f
};
void *g, *l, *p;
h, i, k, m, n, o, q, s, t;
*r;
struct d *u();
v(*w, struct a *x) {
  int a, j;
  if (s && y() || s == t)
    z((int)w);
  u()->f = p;
  if (i == k && aa())
    goto ab;
  if (i == k)
    a = ac();
  for (; m; a++)
    aa(r[a], a);
  j = m;
  for (; j < n; j++)
    ;
  if (aa())
    x->c = l;
  else
    x->c = g;
  if (s == h) {
    x->c = g;
    for (; j < o;)
      a &&ad();
  }
  if (s == h)
    ae();
  0 != af((int)w);
  q = x;
ab:
  z((int)w);
  u()->f = u()->f;
}
