struct a {
  int *arg;
  long b
};
void c() {
  int d[4];
  struct a *e;
  for (; e->b; e++) {
    int f = 0;
    for (; f < 4; f++)
      if (e)
        if (d[f])
          goto fail;
    switch (e->arg[0])
    case 7:
      g();
    if (strcmp(e->b, ""))
      h();
    return;
  fail:;
  }
  g(d[0]);
}
