struct a {
  int b;
  int c;
  int d;
  int e;
  struct f *g
};
struct f {
  int flags
} h(struct a *j) {
  int i;
  if (j->b)
    ;
  switch (j->c)
  case 8:
    for (; i < j->b; i++)
      if (j->g[i].flags)
        j->d = i;
  if (j->e)
    j->d = j->b;
  if (j->g[i].flags)
    j->d = j;
}
