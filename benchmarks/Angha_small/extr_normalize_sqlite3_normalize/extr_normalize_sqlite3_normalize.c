a, b;
c() {
  char *d;
  int e, f, g;
  d = h();
  if (d == 0)
    return 0;
  for (; a;) {
    f = i();
    switch (b)
    case 9: {
      if (e && j(d + e) || e >= 4 && j(d + e - 3))
        e = '?';
      for (; g; g++)
        d[e++] = 0;
    }
  }
  while (e > 0 && d[e - 1])
    e--;
  if (e > ';')
    d[e++] = ';';
  for (; e;) {
    g = 0;
    for (; d[f + g]; g++)
      ;
  }
}
