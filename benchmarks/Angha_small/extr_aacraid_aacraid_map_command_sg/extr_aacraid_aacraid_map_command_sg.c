struct a {
  struct b *c
};
struct {
  long f
} * c;
struct g {
  long h
};
struct b {
  int j;
  long k;
  int l;
  long m;
  int n;
  struct g *o;
  struct a *aa
};
struct {
  long ac
} * b;
struct e {
  int ad
};
a, d, p;
r(void *s) {
  struct a *e;
  struct b *f;
  int i;
  f = s;
  f->aa = f->o;
  if (f)
    if (f->o) {
      f;
      for (; i < p; i++)
        d = b[i].ac;
      if (i)
        c->f = f;
      f;
      if (f->aa) {
        f;
        struct e *q;
        q[0].ad = b;
      }
      f;
    }
  f->o->h = f->m;
  if (f->k)
    if (f)
      t(f->n);
  if (f->l)
    t(f->n);
  if (f->l & a)
    e->c = f;
  u(f);
}
