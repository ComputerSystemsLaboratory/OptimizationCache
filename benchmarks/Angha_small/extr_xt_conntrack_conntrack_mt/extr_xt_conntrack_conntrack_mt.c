struct a {
  int b
};
c, d, e, f, g, h, i, j;
k() {
  struct a *a = k;
  int l;
  if (j)
    if (c)
      l = i;
    else
      l = h;
  if (a->b) {
    m();
    if (l)
      return 0;
  }
  if (j)
    return a->b;
  if (a->b & d && n())
    return 0;
  if (a->b & f)
    if (o())
      return 0;
  if (a->b & e)
    if (p())
      return 0;
  if (a->b & g)
    if (q())
      return 0;
  if (a->b)
    return 0;
  return 1;
}
