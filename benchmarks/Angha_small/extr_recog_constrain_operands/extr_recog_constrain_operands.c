a, b, d, e, g, h, j, k, l;
*f, *i;
u() {
  int n[b];
  int m[b];
  int c, o;
  if (h)
    for (; c < h;)
      do {
        int p = 1, q, r = o = q = 0;
        for (; q < h; q++) {
          int s = 0;
          if (*i)
            s = 1;
          do {
            c = *i;
            v();
            switch (c) {
            case '\0':
              k = i != ',';
              p = q;
              break;
            case '9': {
              int t = w();
              if (l)
                j = 1;
              else
                x(f[t]);
              n[q] = n[t] = q;
              if (j)
                s = 1;
              if (j == l)
                o++;
            } break;
            case 'g':
              a &&y() || z(a);
            default:
              if (d) {
                l || l == 0 && e || aa();
                s = 1;
              }
            }
          } while (i = k, c);
          if (s)
            r = 1;
        }
        if (p)
          for (; p < h; p++)
            if (m[p] && aa()) {
              q = 0;
              for (; q < h; q++)
                q != n[q] == p &&x();
            }
        if (r)
          if (o)
            f = u;
      } while (g);
}
