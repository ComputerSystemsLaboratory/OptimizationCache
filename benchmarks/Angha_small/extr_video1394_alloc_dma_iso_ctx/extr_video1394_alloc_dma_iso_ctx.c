struct e {
  int f;
  int aa;
  int g;
  int h;
  int ab;
  int j;
  int ac;
  int k;
  int l;
  int ad;
  void *m;
  void *n;
  void *o;
  void *p;
  void *q;
  int *r;
  long s;
  long t;
  long u;
  int *v;
  long w;
  int x;
  int ae;
  int af;
  int ag
};
y, z, a, b, c, e, f;
void *ah();
ai() {
  struct e *d;
  int i;
  d = ah();
  if (d)
    aj();
  d->f = e;
  d->aa = ai;
  d->h = f;
  ak();
  d->j = al(d->ag);
  am(d->af);
  an(d->ae);
  if (y)
    ao(&d->x, e, a, (long)d);
  else
    ao(&d->x, e, b, (long)d);
  d->ac = ap(d);
  if (y) {
    d->u = d->t;
    d->s = d->w;
    al();
    d->k = d->ab;
    d->l = z;
    for (; i < d->g; i++) {
      al();
      aq(d);
      d->v[i] = d;
    }
  }
  d->t = d->s = d++;
  aq(d);
  d->r[i] = d;
  if (d->q || d->p || d->o || d->n || d->m)
    ar(d->ad);
  al(c ?: d->g, d->k);
  return d;
}
