struct a {
  struct b *c;
  void *d;
  void *e;
  void *f
};
struct b {
  long d;
  void *e;
  void *f
};
g;
h(i) {
  int j, l;
  unsigned k;
  long a, m;
  struct a *n;
  j = o();
  if (j && j != 3 && j != 4)
    return 0;
  k = 0;
  if (g) {
    m = h();
    a = k = l = j + j--;
  }
  if (j == 4) {
    p();
    j--;
  }
  if (j)
    q();
  if (k)
    n = r();
  if (h > 0)
    n->f = h;
  else
    n->f = n->c->f;
  if (h > 0)
    n->e = h;
  else
    n->e = n->c->e;
  if (h > 0)
    n->d = h;
  else
    n->d = n->c->d;
  s(l);
  t(m, a);
}
