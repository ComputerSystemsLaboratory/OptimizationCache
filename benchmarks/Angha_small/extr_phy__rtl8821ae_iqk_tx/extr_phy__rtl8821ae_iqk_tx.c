struct a {
  int b
};
c, d, e;
f() {
  struct a *g = k();
  int h, i, a = 0;
  int j[c];
  while (a < c) {
    switch (e) {
    case 8:
      if (g)
        l(d, 3212, 6);
      else
        l(d, 3212, 0);
      if (g->b)
        l(d, 3212, 1075199638);
      else
        l(d, 3212, 6);
      j[a];
      i = 0;
      while (1) {
        h = m();
        if (h)
          break;
        i++;
        if (i == 10)
          break;
      }
    }
    a++;
    for (; 0;)
      ;
  }
}
