char a;
b, c;
*d;
j() {
  int e, i, g, h;
  char f;
  while (c) {
    for (; d;) {
      if (f == a)
        ++d;
      ++g;
    }
    e = k();
    break;
  }
  if (e)
    b = c;
  while (*d && i) {
    l();
    i--;
  }
  if (*d && *d != ';')
    while (!g) {
      if (*d)
        h = !h;
      if ((d == '(' || *d == '[') && h)
        ++g;
      if (d == ')' || *d && h)
        --g;
      l();
    }
}
