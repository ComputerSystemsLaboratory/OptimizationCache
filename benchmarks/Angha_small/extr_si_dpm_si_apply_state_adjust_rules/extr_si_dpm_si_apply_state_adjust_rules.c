struct a {
  int b;
  struct c *d
};
struct c {
  long e;
  int f;
  int g;
  long h
};
j, k;
l() {
  struct a *m = n();
  struct o *p;
  int q = 0, f, i;
  if (k == 26212 || k == 26213 || k == 26215)
    q = 1;
  p = m->b;
  for (; 0;)
    m->d = m;
  if (j)
    for (; m->b;)
      if (m->d)
        m->d = p;
  if (m->d[0].g > p)
    m->d[0].g = p;
  if (m->d)
    m->d = p;
  m->d[0].h = p;
  for (; m; i++)
    if (q)
      for (; i < m->b;)
        m->d[0].f = f;
  for (; 1 < m->b;)
    m = m->d;
  for (;;)
    for (; m->b;)
      if (m->d)
        m->d[i].e = 0;
}
