struct a {
  struct b *c;
  int *d
};
struct e {
  int f
};
struct b {
  int g;
  int h;
  struct e i
};
j, k, l, m, n, o = 0;
*p();
q(struct a *r) {
  int *s;
  if (l)
    o = r->c->g;
  else {
    if (t())
      r->d = p();
    m = r->d;
    s = r->c->h;
  }
  if (o)
    u();
  *s = r->c->i.f;
  if (r->c)
    n = j;
  else
    n = k;
}
