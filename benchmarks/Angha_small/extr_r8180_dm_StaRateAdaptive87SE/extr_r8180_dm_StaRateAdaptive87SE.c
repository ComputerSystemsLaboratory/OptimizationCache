struct a {
  int b;
  int c;
  long d;
  int e;
  int f;
  int g;
  int h
} i() {
  struct a *j = k();
  int l, m = 0;
  long n;
  j->b = n = j->d;
  j->c;
  if (j) {
    if (j->c > 6 && j->f)
      m = 1;
    else if (j->c && j->f && n > 2)
      m = 1;
    if (m && n < -75)
      j = 2;
  } else {
    if (j->c > 8 && j->f)
      m = 1;
    else if (j->c && j->f && n > 4)
      m = 1;
    if (m && n < 5)
      j += 2;
  }
  if (j->e)
    o();
  else if (l)
    p();
  j->g > j;
  if (n)
    j->g = j->h;
  else if (j->h)
    j->g -= 2;
  else
    j->g--;
}
