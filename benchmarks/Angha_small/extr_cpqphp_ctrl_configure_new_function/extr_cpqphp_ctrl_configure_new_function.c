struct a {
  struct b *c;
  struct b *d;
  struct b *e
};
struct b {
  int f;
  struct b *i
} * j, *l, *q, *r, *s, *t;
struct {
  int a;
  struct b *d;
  struct b *c;
  struct b *e
} * u;
struct g {
  int *aa
};
k, m, n, o, p;
*v;
w() {
  int a, b;
  struct g h;
  struct a c;
  c.d;
  c.c = &h;
  if (m) {
    a = 0;
    for (; a < 4; a++)
      if (x(a)) {
        y(u, a + 1, h.aa[a]);
        if (o)
          goto ab;
      }
  }
  s->i = t;
  if (c.e) {
    if (v)
      r->i = u;
    u->e = r;
  }
  u->e = r;
  if (l && c.d) {
    z();
    if (j)
      l->i = u->d;
  } else if (l)
    l->i = u->d;
  u->d = l;
  if (q && c.c) {
    z(c);
    q->i = u->c = q;
    ac();
    q->i = u->c = q;
  } else if (q) {
    q->i = u->c;
    u->c = q;
    ad();
    p = v - p;
    j->i = u->d;
  }
  if (n)
    n;
  else
    b = k;
  ae(b);
ab:;
}
