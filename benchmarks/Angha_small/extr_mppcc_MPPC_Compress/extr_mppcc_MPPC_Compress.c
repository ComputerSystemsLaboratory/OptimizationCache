typedef a;
struct b {
  int c;
  int d
};
e, f, g, h, j, k;
l() {
  struct b *m = l;
  int i;
  a *n, *o;
  if (m->d + f)
    p(m->c, j, e);
  k = m->d + f;
  while (i) {
    if (o++) {
      ++i;
      continue;
    }
    while (*n == *o)
      while (*n++)
        ;
  }
  while (f)
    ;
  if (h && h != 8)
    q();
  if (f < g)
    r();
  f += g;
}
