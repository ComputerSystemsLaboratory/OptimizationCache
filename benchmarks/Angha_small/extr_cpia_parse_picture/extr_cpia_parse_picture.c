typedef a;
struct b {
  int c;
  int count
};
d, e;
struct f *i;
struct f {
  int g;
  struct b h
} m() {
  a *j, *a;
  int k, l = i->h.c;
  while (e)
    while (k > 1) {
      ++a;
      if (a[0])
        e -= 4;
      j = l;
      if (e)
        k = d;
    }
  i->h.count = j - &i->h.c;
}
