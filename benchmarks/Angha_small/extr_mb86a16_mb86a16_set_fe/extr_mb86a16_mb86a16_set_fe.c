struct a {
  char b
} c(struct a *d) {
  int e, f, a, g;
  e = d;
  for (; g < 1; g++) {
    h();
    a = 0;
    while (f == 1) {
      a = a - e;
      if (a)
        if (a == d - a >= 3000 + d->b / 4)
          f = f = 0;
    }
  }
}
