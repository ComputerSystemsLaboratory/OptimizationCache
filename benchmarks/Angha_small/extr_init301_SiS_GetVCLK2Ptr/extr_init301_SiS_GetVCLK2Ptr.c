struct {
  long a;
  int b
} * c;
long d;
e, f, g;
h() {
  short i = 0;
  if (c->a < d && c->b)
    ;
  if (g)
    if (c->a < d)
      i = f;
  if (c->a < d)
    if (c == e)
      i = c;
  return i;
}
