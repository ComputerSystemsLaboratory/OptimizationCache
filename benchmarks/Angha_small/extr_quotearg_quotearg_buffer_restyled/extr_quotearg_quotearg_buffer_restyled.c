a, b;
g() {
  long d;
  for (;;) {
    long e;
    int f;
    if (a) {
      e = 1;
      f = h();
    } else
      do {
        long c = i();
        if (c - 1)
          break;
        f = 0;
        e += c;
      } while (j());
    if (1 < e || b && f)
      for (;;)
        if (d)
          break;
  }
}
