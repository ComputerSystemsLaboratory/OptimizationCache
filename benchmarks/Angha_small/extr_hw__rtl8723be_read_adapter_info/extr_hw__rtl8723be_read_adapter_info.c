struct a {
  long b;
  int aa
};
struct c {
  int d;
  int e;
  int f;
  int g;
  int aa
} * h;
long i, n, o;
*j, *l, *m, *p;
k, q, r;
s() {
  struct c *a = u();
  struct a *b = v();
  int t = 0, c = 0, d = 0;
  if (q)
    a->aa = w();
  b->aa = a->aa;
  x(a->aa);
  if (b->b) {
    for (; y();) {
      t = c = 1;
      break;
    }
    for (; y();)
      if (a->d) {
        d = 1;
        break;
      }
    switch (a->e)
    case 1:
      if (a->f == 6)
        if (a->g == 4332 && t)
          b->b = p;
        else if (a->g)
          b->b = h;
        else if (a->g == 4332 && d)
          b->b = n;
        else if (a->g == 4332 && r)
          b->b = i;
        else {
          b->b = k;
          if (a && a->d)
            b->b = m;
          else if (a->g == 2 && a)
            b->b = l;
          else if (a->g == 5)
            b->b = j;
          else
            b->b = o;
        }
      else if (a->f)
        if (c)
          b->b = p;
        else if (a->g == 5)
          b->b = h;
        else if (a->g == 2 && a)
          b->b = l;
        else if (a->g && a == 6)
          b->b = j;
        else
          b->b = o;
      else
        b->b = o;
  }
}
