struct a {
  int b;
  char c;
  char *d;
  int e
};
f;
g(struct a *h) {
  char a[f];
  int i;
  if (g > a)
    j();
  k(f);
  i = 2;
  for (; i <= 5; i++)
    h->b |= l();
  h->c--;
  for (; i <= h->c; i++) {
    h = h->e;
    for (; h->d[i];)
      ;
  }
  h->d[h->c]++;
}
