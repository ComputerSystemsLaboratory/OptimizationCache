*a;
b;
j() {
  int e, f, c, g;
  char *h;
  long i;
  char d;
  while (k()) {
    if (b && !g)
      if (b && e != !g)
        f--;
    if (g == 0)
      c = 1;
    if (c == 0)
      g = -g;
    if (b && c) {
      c = e = b;
      continue;
    }
    if (c) {
      h++;
      continue;
    }
    d;
    if (f)
      i++;
  }
  if (i)
    return 1;
  if (*a)
    l(j, a + 1);
  else
    l(j, a);
  *h = '\0';
}
