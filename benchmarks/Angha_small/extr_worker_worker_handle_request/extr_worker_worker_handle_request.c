struct a {
  long b;
  int aa;
  int c;
  int d;
  int f;
  int g
};
struct h {
  int i
};
struct j {
  struct a ab;
  int k;
  struct h l
};
struct m {
  long n;
  long ac
} o;
struct {
  int ad;
  int addr
} p, *r;
q;
s(void *t) {
  struct j *j = t;
  int *e;
  struct m a, b;
  u(r->addr, r->ad);
  j->l.i++;
  v(j->ab.g, j);
  j->ab.aa &&w(j->ab.f, j);
  j++;
ae:
  if (x(j)) {
    e = y(j->ab.d, &a);
    if (z(j, e)) {
      if (j->ab.aa || j->ab.aa && j) {
        j < j;
        af(e);
        ag(j->k);
        j;
        return 0;
      }
      b.n = o.n;
      b.ac = o.ac;
      a = b;
      goto ae;
    }
    if (q)
      ah(p, "", &r->addr, r->ad);
    else
      ah(p, "tcp request from", &r->addr, r->ad);
  }
  ai(j->ab.c);
  j;
}
