struct a {
  long b;
  int c
};
d, e, f;
g(struct a *h) {
  int i = 0;
  for (; i < h->b; i++)
    if (e && j())
      break;
  if (i >= h->b)
    while (k()) {
      f = 0;
      while (l(h->c & f))
        ;
      if (d)
        continue;
      m();
    }
}
