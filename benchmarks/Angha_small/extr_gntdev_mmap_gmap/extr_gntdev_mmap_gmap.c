struct a {
  struct b *c
};
struct b {
  long d
};
long e;
f;
g(struct a *h) {
  long i;
  if (h)
    for (;; i++) {
      int a;
      while (a && h->c[i].d == e) {
        j(f, &h->c[i]);
        a++;
      }
      if (h->c[i].d == e)
        return 0;
    }
}
