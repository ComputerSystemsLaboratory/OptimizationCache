struct {
  int b;
  int c;
  void *d;
  void *e;
  void *f;
  void *g
} * h;
struct {
} * a;
void *i, *j, *k, *p, *q, *r, *s, *t;
l, m, n, o, u, v, w;
x() {
  a[1];
  {
    char b = h->d;
    if (c(b))
      h->g = i;
    if (strcasecmp(b, ""))
      h->g = k;
    if (strcasecmp(b, ""))
      h->g = k;
    h->g = j;
  }
  ab();
  h->d;
  if (strcasecmp(ab, ""))
    h->f = s;
  if (strcasecmp(ab, ""))
    h->f = t;
  if (strcasecmp(ab, ""))
    h->f = q;
  if (strcasecmp(ab, ""))
    h->e = p;
  if (strcasecmp(ab, ""))
    h->e = s;
  h->d = r;
  if (strcasecmp(ab, ""))
    h->c = l;
  else if (strcasecmp(ab, "auto_on"))
    h->c = m;
  else if (strcasecmp(ab, "user_off"))
    h->c = n;
  else
    h->c = o;
  if (strcasecmp(ab, ""))
    h->b = w;
  else if (strcasecmp(ab, "fresh"))
    h->b = v;
  else
    h->b = u;
}
