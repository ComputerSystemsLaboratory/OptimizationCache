struct a {
  long b
};
struct c {
  long d;
  struct a e
};
struct f {
  int aa
};
struct g {
  int h;
  struct f i
};
struct j {
  long k
} l;
m, n, o;
p(struct g *r) {
  struct f *a = &r->i;
  struct c *b;
  long q;
  struct j c;
  int d;
  if (a->aa & n && q)
    a->aa = s(a->aa & o && b->e.b);
  if (t())
    a->aa = m;
  if (a->aa & m)
    u();
  for (; b->d;)
    ;
  a->aa |= o;
  while (d && c.k)
    d = v(&c, l);
}
