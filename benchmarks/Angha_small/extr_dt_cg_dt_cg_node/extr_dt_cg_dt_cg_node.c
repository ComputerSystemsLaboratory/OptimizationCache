struct a {
  int b;
  int c;
  int d
};
e, f, g, h, i, j, k, l, m, a;
n(struct a *o) {
  p(o ? h : o->c ? j : l ? i : a ? f : g);
  q(m ? e : o->c ? h : k);
  p(o ? j : l);
  o->b = 0;
  if (o)
    r();
  s(o->b);
  if (o->d)
    t();
  u(o->d);
}
