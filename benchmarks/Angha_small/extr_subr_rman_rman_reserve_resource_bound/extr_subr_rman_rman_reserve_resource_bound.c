struct a {
  int b;
  int c;
  int d;
  int *e
} f(*), *h, *i;
g;
j(l) {
  struct a *k;
  if (h)
    f(0);
  else
    f(l - 1);
  h = m();
  for (; h && h->b < l - 1; h = 0)
    f(l - 1);
  for (; k; k = m())
    ;
  for (;; k = m)
    if (k->d && k->c && l) {
      n();
      i = k->c;
      if (k->e)
        p();
      k->d = g;
      i->e = k->e;
      goto o;
    }
o:;
}
