struct a {
  void *b
} * c;
d, e;
*f;
bcopy(struct a *, struct a *, int);
g(h, i) {
  struct a j, a, k;
  int b;
  f = c;
  for (;;) {
    if (b)
      if (i)
        b = 20;
      else if (d)
        ;
      else
        b = 0;
    if (d && i == 0)
      l();
    if (h && i) {
      m(&j);
      m(&a, &k);
    }
    if (h)
      bcopy(&j, e, sizeof(e));
    bcopy(&a, &k, sizeof(k));
  }
}
