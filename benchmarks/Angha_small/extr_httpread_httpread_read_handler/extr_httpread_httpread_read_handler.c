struct a {
  int b;
  long d;
  int e;
  int f;
  char *g;
  int i;
  long j;
  char *aa;
  int k;
  int l;
  long m;
  int n;
  int ab;
  int (*ac)(*, int, int);
  int o;
  long ad;
  long p
};
q, r, s, t;
u(void *v) {
  struct a *h = v;
  int a;
  char *b, *d;
  char e[s];
  w(t, h);
  a = x(h->o, e);
  if (a) {
    if (h->ad || h->p)
      w(h->d, h->e);
    h->f = 1;
  }
  if (h) {
    d = h->g + h->i;
    for (;;) {
      if (a == 0)
        goto ae;
      if (h >= 4 && d - 1 - 2 == '\r' && d)
        break;
    }
    if (h->j)
      if (h->p && h->e)
        w();
  }
  if (h->ad && h->k)
    for (;;) {
      int c;
      if (a <= 0)
        break;
      c = *b++;
      a--;
      switch (h->l) {
      case 0:
        if (c)
          h->l = 1;
        h->l = 9;
      case 1:
        if (c)
          h->f = 1;
      case 8:
        if (c)
          h->l = 0;
        h->l = 9;
      }
    }
  h->ac(h, h->ab, q);
ae:
  y(h->m);
  z(h->aa, h->d);
  h->n = h->ac(h, h->ab, r);
}
