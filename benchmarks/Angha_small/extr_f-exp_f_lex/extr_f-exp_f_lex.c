a, b;
char *d;
*e, *f;
j() {
  int c;
  char *h;
  e = d;
  h = d;
  for (; a;)
    switch (c = *h) {
    case 0:
      return 0;
    case '9': {
      int i = 0, g = b > 0;
      if (c == '0' && (h == 'x' || h[1]))
        ;
      else if (c == '0')
        g = 0;
      for (;; ++f)
        if (g && !i && (f == 'e' || f == 'E'))
          i = 1;
    }
    }
  for (;;)
    if (h[0] >= 'a' && h[0] < b - 10 || h[0] >= 'A' && h[0] < b)
      k();
}
