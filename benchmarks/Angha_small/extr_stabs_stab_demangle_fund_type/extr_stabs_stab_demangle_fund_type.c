typedef a;
b, d, e;
**c;
f() {
  int *g;
  a constp, h, done;
  while (!done)
    switch (**c) {
    case 'C':
      constp++;
    case 'U':
      d = c;
    case 'S':
      e = c;
      h = done = b;
      g = e ?: "";
    case 't':
      i();
      if (g)
        *g = 0;
    }
  if (constp)
    if (h)
      *g = 0;
}
