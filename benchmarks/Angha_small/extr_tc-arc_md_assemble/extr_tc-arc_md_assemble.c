struct b {
  int c
};
d, f, g;
*e, *i;
char h;
y() {
  char *b;
  int c;
  char k;
  long j;
  static l;
  l = 1;
  while (z())
    b++;
  for (; c; c = aa()) {
    int o, m, p, q;
    char *n;
    int *r;
    o = q = 0;
    n = g;
    for (; *n;) {
      struct b *s;
      if (n != *++n) {
        if (b == e)
          o = 1;
        else
          break;
        continue;
      }
      while (ab(e))
        s = e[*e];
      if (s) {
        j = ac(j);
        if (h) {
          k = h;
          if (s->c)
            ;
          break;
        }
      } else if (o) {
        int t;
        if (b)
          continue;
        if (i)
          q = 1;
        else
          ad();
        t = 0;
        if (q && f == n)
          b = 1;
        for (; i; ++i)
          if (f == *e) {
            t = 1;
            break;
          }
        r = i;
      } else {
        b = 4;
        if (ae())
          break;
        ++m;
      }
    }
    if (*n == '\0') {
      long u = af();
      static v, w;
      int x = d;
      int a = c;
      if (v && u)
        ag();
      v = x != d;
      if (r && w)
        ag();
      w = !u;
      for (; m;)
        p;
    }
  }
  if (k)
    ah();
}
