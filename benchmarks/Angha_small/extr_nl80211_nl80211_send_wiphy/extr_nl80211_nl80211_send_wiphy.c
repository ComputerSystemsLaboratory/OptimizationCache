struct a {
  int b
};
struct c {
  int d
};
enum e { f } g;
h, j, k, l, n;
*m;
o() {
  struct a *a;
  int p, i;
  enum e b;
  if (n && q())
    goto ac;
  if (n & j && q())
    goto ac;
  if (n & h && q())
    goto ac;
  if (n & k && q())
    if (n)
      goto ac;
  if (n)
    b = a->b;
  for (; b < g; b++) {
    struct c *c = b;
    for (; c->d; i++) {
      r();
      if (s())
        break;
    }
    if (p)
      b--;
  }
  for (; i < l; i++)
    r(i, m[i]);
ac:;
}
