a, b, c;
struct bmap {
  int d;
  int e;
  int *f
} g() {
  int h, i, e;
  struct bmap *d = a;
  d->d = 1 << b;
  e = d->e >> b;
  d = c % d->d ? 1 : 0;
  for (;;)
    for (; i && h < e; i++, h++)
      d->f = d->f[h];
}
