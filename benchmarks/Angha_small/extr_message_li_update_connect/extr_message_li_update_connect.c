typedef a;
struct {
  int *b
} * c;
d, e, f, g, h, i, j;
k() {
  a l, m = h;
  if (i)
    c = e;
  if (c[l].b[m] & d) {
    for (; j < f; j++)
      if (j)
        c[l].b = d;
    for (; j < f; j++)
      if (g)
        c[g].b = d;
  }
  if (c[m].b[l] & d)
    for (; f; c)
      for (; j < f; j++)
        if (j)
          c[j].b = d;
}
