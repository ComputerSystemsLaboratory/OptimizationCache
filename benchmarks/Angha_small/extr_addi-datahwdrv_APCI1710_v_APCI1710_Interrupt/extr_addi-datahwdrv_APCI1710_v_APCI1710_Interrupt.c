struct {
  struct b *c
} * j;
struct b {
  long d
};
struct e {
  long f
};
struct g {
  int h;
  long i
};
struct {
  struct e k;
  struct g l
} * b;
struct {
  int m;
  int n;
  int o
} * d;
p, q, a, c, r;
s() {
  unsigned e = 0, f, g;
  for (; e < 4; e++) {
    if (b->l.h)
      t();
    if (b->l.h == q)
      t();
    if (b->l.h)
      for (; f < 2; f++)
        if (b)
          b->k.f = 0;
    if (b) {
      g = 0;
      for (; g < 2; g++)
        if (j[e].c[g].d == p) {
          t(b->l);
          if (r)
            t(b + 6 * g + 4 * e);
          d[b->k.f].m = 4096 << g;
          d[b->k.f].n = d[b->k.f].o = u(a, c, 0);
        }
    }
    v(b);
  }
}
