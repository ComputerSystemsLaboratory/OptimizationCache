struct a {
  struct b *aa
};
struct c {
  int *d;
  struct a e
};
struct f {
  int g;
  struct h *i
};
struct j {
  int af;
  long g;
  struct j *ag
};
struct k {
  long g;
  int l;
  struct k *ag
};
struct b {
  long m
};
struct h {
  int i;
  int aa
};
n, o, p, q;
r(struct c *s, struct f *t) {
  struct k *a, *b;
  struct j *c;
  static long d;
  long e;
  int f, g, h;
  if (t->i->i & p)
    u();
  if (t->i->i & (p | o))
    s = d;
  switch (t->g)
  case 8:
    for (; t->i; g++) {
      s->d = e;
      if (g + 1 < t->i->aa)
        e = s->e.aa[g].m;
      if (f) {
        f--;
        continue;
      }
      if (c == 0)
        continue;
      f = c->af;
      if (g)
        c = c->ag;
    }
  do {
    g &&v();
    if (h || b->l == r)
      h = c && n ? 2 : c->g;
    if (c)
      a = a->ag;
    if (s->d)
      w();
    s = s->d;
    q = n ?: a && a->g || c && c->g ? 1 : 0;
    if (t->i && v())
      t = t == 8 ? 2 : t || t->i ? 1 : 0;
  } while (1);
}
