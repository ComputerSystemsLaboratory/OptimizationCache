struct a {
  long b;
  long c
};
enum d { e } f;
g, h, i, j;
struct a *k(l) {
  enum d a = m();
  struct a n;
  if (i)
    n = *k();
  h || f;
  if (n.b && o(&n) || n.b == p())
    if (g && q())
      ;
    else if (a == g)
      if (n.b && r() || n.b == j && s())
        ;
}
