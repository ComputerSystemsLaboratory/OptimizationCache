struct a {
  int b;
  int d
} * e;
f;
g(struct a *h) {
  long j = 0, i = f;
  enum { k, l, m, n, o, p, q } b = b;
  while (i < j) {
    unsigned c = e++;
    switch (b) {
    case k:
    case l:
      switch (c)
      case '\t':
      case '\r':
        continue;
      b = m;
      continue;
    case p:
      switch (c) {
      case '9':
        if (h->d >= 4)
          b = q;
      case 'F':
        if (h->d >= 4)
          b = h->d++;
        if (r())
          b = j = i;
      }
      break;
    case o:
      switch (c) {
      case '9':
        if (h)
          b = b = j = i;
        continue;
      case '\n':
        b = 1;
      }
      break;
    s:
      b;
      b;
    case n:
      switch (c) {
      case '.':
        if (b == b)
          b = q;
        if (h)
          b = q;
        else {
          b = q;
          continue;
        }
        if (b)
          b = b;
      }
    }
  }
  h->b = b;
}
