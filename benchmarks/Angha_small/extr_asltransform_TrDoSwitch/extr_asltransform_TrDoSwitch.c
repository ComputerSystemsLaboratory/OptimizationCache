a, b, c;
*d;
e() {
  int f;
  int *g, *h, *i;
  while (h) {
    h = d;
    if (c) {
      if (g)
        i = j();
      g = i;
    } else if (a)
      if (f)
        k();
      else
        f = b;
  }
}
