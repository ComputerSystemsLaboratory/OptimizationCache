a, b, c, d, e, f, g;
h() {
  if (c == 1 && i())
    ;
  else
    c &&j();
  switch (b)
  case 1:
    if (f && k())
      b = 0;
  if (f)
    f = b;
  if (b)
    l();
  if (e)
    e = g && b;
  if (b)
    if (d)
      m();
  a = d;
}
