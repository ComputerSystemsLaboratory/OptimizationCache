struct a {
  long c;
  int d
};
e(f, g, h) {
  long i;
  struct a *b;
  int j;
  h && b->d;
  if (h && k())
    g = 0;
  if (h && f)
    i = l(&g);
  if (g)
    return m();
  b->c &&e;
  j = m;
  if (j)
    n(i);
}
