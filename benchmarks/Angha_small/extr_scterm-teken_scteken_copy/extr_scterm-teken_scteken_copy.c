struct a {
  int b;
  int c
};
struct d {
  int b;
  int c
};
struct e {
  struct a f;
  struct d g
};
struct {
  int c;
  int b
} * h;
i;
*l;
struct j {
  int k
} m(struct e *n) {
  struct j *o = l;
  int p, a;
  if (n->f.b && n->g.b)
    q();
  else
    i = n->g.b;
  if (h) {
    o;
    a = o->k;
  } else
    p = o->k;
  while (p)
    ;
  r((h->c + n->g.c - n->f.c - 1) * o->k + (h->b + n->g.b - n->f.b - 1));
}
