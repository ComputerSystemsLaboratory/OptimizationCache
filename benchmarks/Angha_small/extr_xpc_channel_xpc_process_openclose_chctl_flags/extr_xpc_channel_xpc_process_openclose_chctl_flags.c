struct {
  long b
} * n;
struct c {
  int ab;
  int d
};
enum e { f } g;
h, i, j, k, l, o, p, q, r;
*m;
s() {
  struct c *a = &m[p];
  enum e b;
  int t = 0;
  if (a && k) {
    a->d = q;
    goto ag;
  }
  if (g) {
    if (j) {
      u();
      goto ag;
    }
    if (l || a->ab)
      goto ag;
    if (k) {
      a->d = h;
      goto ag;
    }
    if (n == 0 || n->b)
      goto ag;
    if (o) {
      v();
      goto ag;
    }
    if (i)
      goto ag;
    if (b) {
      v();
      goto ag;
    }
    w(a);
    t = 1;
  }
ag:
  x(r);
  if (t)
    y();
}
