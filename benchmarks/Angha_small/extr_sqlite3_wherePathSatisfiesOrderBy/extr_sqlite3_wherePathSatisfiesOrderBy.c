struct b {
  int c
};
struct d {
  int e;
  struct b f
};
struct {
  struct h *k
} * p;
struct h {
  long a
};
l, a, o, b;
*m, *n;
q() {
  int c, d, i, j, f;
  struct d *e;
  d = r();
  for (; c <= q;) {
    e = n;
    if (e->e)
      for (; i; i++) {
        if (o)
          s();
        if (o & a)
          t();
      }
    if (e->e & l)
      b = e;
    if (e->f.c)
      c = u();
    if (e->f.c)
      for (; i < d; i++) {
        r(i);
        v(m[i]);
        struct h g = *p[j].k;
        w(g);
      }
  }
  if (f)
    return d;
  if (c)
    r();
}
