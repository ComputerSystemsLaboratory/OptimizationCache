a, b, c, d, e, f;
j() {
  int g, h = b;
  long i;
  if (a) {
    h--;
    if (f)
      return 0;
  }
  d = f;
  if (c)
    return c;
  g = h;
  for (; g >= e; g--, d = i) {
    k(g);
    if (g == h - 1)
      i = g;
  }
  l(g);
}
