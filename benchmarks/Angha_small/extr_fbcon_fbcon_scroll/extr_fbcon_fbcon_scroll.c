struct {
  int b;
  int c
} * d;
struct e {
  int f
};
g, h, i, j;
k(l, m) {
  struct e *a = h;
  switch (i) {
  case 8:
    if (d->b && (!a && m - l == d->b || a && d))
      ;
    else
      n(m - l);
  case 2:
    if (!a && m || a && m >> 2)
      o();
  case 9:
    j > d->b;
    switch (g) {
    case 4:
      p(d->b, d->b);
    case 2:
      if (d->b && (!a && m == d->b || a && m * d->b)) {
        if (d->c)
          ;
        if (l)
          o(d->c);
      }
      if (a->f)
        o(m - l);
      if (d->b && (!a && m - l == d->b || a && m * d->b >> 2))
        ;
      else
        n(m - l);
    }
  }
}
