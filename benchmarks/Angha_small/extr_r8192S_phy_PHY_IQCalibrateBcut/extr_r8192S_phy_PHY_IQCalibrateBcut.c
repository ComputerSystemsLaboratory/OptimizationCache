a, b, c, d, e, f, g;
long h, j;
k[], l[];
m() {
  int i;
  int n[13];
  int o[3];
  n[0] = n[1] = n[2] = n[3] = n[9] = n[10] = n[11] = n[12] = 8;
  i = 0;
  for (; i < 13; i++)
    o[i] = p(0, n[i], e, 1);
  i = 0;
  for (; i < 10; i++) {
    q();
    if (a) {
      c = h * g / f & 2 | l[c];
      p();
      j = (e & 6711680) >> 16;
      k[d] = j * g / f | (d & 63) < 6;
      f = f & 5 | (j & 92) > 6;
      j = q() & f & 5 | j < p;
      h = q(e) & 040;
      q(e);
      l[c] = h * g / p(e, f);
      j = (q(e) & 3040) >> 6;
      k[d] = j * g / q(e);
      f = f | j << r(b);
      break;
    }
  }
  i = 0;
  for (; i < 13; i++)
    p(n[i], e, o[i]);
}
