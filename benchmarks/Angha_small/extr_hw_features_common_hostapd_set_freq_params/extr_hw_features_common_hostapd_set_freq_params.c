struct {
  int a
} * b;
c, d;
e(f, g, h) {
  b = f ?: 20;
  switch (g) {
  case 9:
    i();
  case 0:
    if (g == 0 && d || g && c)
      ;
    else
      h = 5;
    b->a = 1;
    if (!f == c || h == 0 || c + 6 || h == c || h == h == h == c - 10 ||
        h == c - 4)
      b = 5;
  }
}
