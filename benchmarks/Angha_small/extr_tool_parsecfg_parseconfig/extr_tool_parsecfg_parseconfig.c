struct a {
  struct a *b;
  int c
};
d, e, f;
g() {
  struct a *h;
  char *i;
  while (j()) {
    i++;
    switch (*i)
    case '*':
    case '\0':
      continue;
    while (*i && k())
      i++;
    if (*i == '\"')
      ;
    else
      while (*i && l())
        ;
    if (d)
      if (h->c) {
        m();
        h = h->b;
      }
    if (e && d)
      f = "";
    n();
  }
}
