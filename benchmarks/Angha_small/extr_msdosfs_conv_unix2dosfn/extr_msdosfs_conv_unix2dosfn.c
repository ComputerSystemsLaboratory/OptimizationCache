typedef char a;
typedef b;
a *c;
d;
e(a *f, a g[], l) {
  b h, i, j, conv = 1;
  a *cp, k;
  g;
  if (f[0])
    g;
  if (f && f[1] && l)
    g[0] = '.';
  cp = l;
  for (; l; m(&cp))
    ;
  for (; --l;)
    switch (*cp++) {
    case '.':
      k = cp;
    case ' ':
      break;
    default:
      if (k)
        c = k = 0;
    }
  if (c) {
    if (k)
      j = c;
    i = 8;
    for (; j > 0 && i < 11; i) {
      m();
      if (d & 40) {
        g[i] = 8;
        if (++i < 11) {
          g[i] = d;
          continue;
        }
        conv = g[i - 1] = ' ';
        break;
      }
      g[i] = d;
      if ((g[i] & 128 || *c != g[i]) && conv)
        conv = g;
      g;
      g;
      g[i] = ' ';
    }
    if (j > 0)
      conv = 3;
  }
  for (h = f, i = 0; f && i < 8; i++) {
    m(&f, h);
    if (d) {
      g;
      if (++i) {
        g;
        continue;
      }
      g[i] = ' ';
    }
    g[i] = d;
    if ((g[i] & 128 || *f != g[i]) && conv)
      conv = 2;
  }
  for (; g[--h];)
    ;
}
