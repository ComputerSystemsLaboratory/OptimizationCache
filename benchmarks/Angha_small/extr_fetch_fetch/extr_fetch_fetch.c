a, b, c, e, f, g, h, i, j;
n() {
  int d, l;
  char k[1];
  unsigned m = a;
  if (m)
    o();
  l = p(k);
  if (m)
    l &&r();
  l == 1 || r();
  if (b && s() && c)
    goto q;
  if (c)
    i = 0;
  while (e) {
    j;
    if (d)
      break;
    for (; g; g = t())
      ;
  }
  if (h)
    f &&d;
q:
  u(l);
}
