struct a {
  int b;
  int d;
  int e;
  long f
} i, j;
struct {
  long g;
  long h
} * m;
*k;
l, n;
o() {
  struct a p;
  long q;
  char c = *k;
  if (n)
    r();
  s(k);
  do {
    if (c == '\0' || c == '\n')
      q = -1;
    else
      q--;
    if (c != '\0')
      t();
  } while (q >= 0 && s);
  p = i;
  i = j;
  l = p.e;
  j = p;
  if (m->h)
    u();
  if (m->g)
    m = 0;
}
