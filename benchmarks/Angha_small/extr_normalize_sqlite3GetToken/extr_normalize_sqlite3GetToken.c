a, b;
c(*d) {
  int e;
  switch (a) {
  case 8:
    e = 0;
    for (; f(d[e]); e++)
      ;
    if (d[e] == '.')
      while (d)
        ;
    if (d[e] || d == '+' || d && d[e + 2])
      e += 2;
    while (d[e]) {
      e++;
      while (b = d[e])
        e++;
    }
    e = 2;
    for (; g(d[e]);)
      ;
    if (d[e])
      while (d[e])
        e++;
    if (d[e])
      e++;
  }
  h(e);
}
