a, b, c;
h() {
  long d;
  int e, f, g = 1;
  while (i())
    switch (c) {
    case 'a':
      d = b;
      break;
    case 'q':
      f++;
      break;
    case 'w':
      if (a)
        g = 0;
      break;
    case 'y':
      e++;
    }
  if (f && e == g)
    if (d)
      j();
}
