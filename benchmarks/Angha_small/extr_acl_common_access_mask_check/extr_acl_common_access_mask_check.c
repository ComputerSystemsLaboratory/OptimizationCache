struct {
  int c
} * q;
d, e, f, g, h, i, j, k, l, m, n, o, p, r, a;
t() {
  int b, c, s;
  q == e;
  c = o;
  if (g)
    b = l;
  else if (i)
    b = o;
  else if (f)
    b = j;
  else {
    if (a)
      ;
    else if (s)
      m;
    else if (q->c & r > t & d)
      return k;
    if (h)
      b = n;
    else
      return 0;
  }
  if (c & b)
    if (q)
      return p;
  return 0;
}
