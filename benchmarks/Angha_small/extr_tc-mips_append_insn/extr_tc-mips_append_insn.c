struct a {
  int b;
  long c;
  int d;
  struct e **aa
};
struct e {
  int f;
  int g
};
h, j, k, l, m, n, a, b, c, d, e, f, g, o;
struct e *q();
r(*s) {
  struct a *p = 0;
  if (g != 2 && f) {
    t();
    int i;
    for (; i; i++)
      u();
  }
  if (g != 2)
    if (o && *s)
      ;
    else {
      int i;
      if (s[0] == h || s[0] == 137 || s[0] == 129 || s[0] == 135 ||
          s[0] == 134 || s[0] == l || s[0] == a || s[0] == m || s[0] == j ||
          s[0] == k || s[0] == d || s[0] == 130 || s[0] == 131 || s[0] == c ||
          s[0] == b || s[0] == n)
        p->aa = i = 1;
      for (; i < 3; i++)
        if (s[i]) {
          p->aa[i] = q(p->d, p->c, p->aa[0]->g, e);
          p->aa[0]->f = 1;
          p->aa[i]->f = 1;
        }
    }
}
