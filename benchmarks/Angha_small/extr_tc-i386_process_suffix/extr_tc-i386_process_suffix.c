a, b, c, f, g, h, i, j, k, l, n, o, p, r;
long d, e, m, q;
t() {
  if (l)
    if (k)
      q = m;
    else if (r)
      q = e;
    else
      q = d;
  if (q) {
    if (p)
      q = m;
    if (p == 0)
      if (o & i)
        q = b;
    for (;;) {
      q = o ? m : h ? e : d;
      break;
    }
  }
  if (r)
    if (j)
      if (p & (n ? f : g))
        if (u())
          return 0;
  int s = c;
  if (r)
    s = a;
  u(s);
}
