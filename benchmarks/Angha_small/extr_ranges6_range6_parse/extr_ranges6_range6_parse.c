struct a {
  long b;
  long c
};
struct d {
  struct a e;
  struct a begin
};
f(char);
long g;
long h;
struct d i(*buf, j) {
  unsigned k;
  struct d l;
  static struct d badrange;
  if (0 < isspace)
    m(&k, l);
  l.e = l.begin;
  int n;
  unsigned digits = 0;
  k++;
  while (k < j && f(buf[k])) {
    n = n * 10 + buf[k++] - '0';
    if (++digits > 3)
      return badrange;
  }
  if (n)
    h = g;
  l.begin.b &= h;
  l.begin.c &= g;
  l.e.b = l.begin.b | ~h;
  l.e.c = l.begin.c | ~g;
  if (o()) {
    l.begin.b = l.begin.c = ~0ULL;
    l.e.b = l.e.c = 0;
  }
  fprintf(l.e);
}
