struct {
  int a
} * b;
c, d;
e() {
  int f = 0;
  if (d && b->a || b)
    if (b->a == 1 || b->a && (d == 26211 || d == 26213 || d == 26215))
      f = 1;
  if (c)
    g();
  h(c);
  if (f)
    i();
}
