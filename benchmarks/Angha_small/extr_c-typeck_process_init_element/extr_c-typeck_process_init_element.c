struct a {
  long b;
  long c
};
struct {
  struct a d
} * e;
f, g, h, i, j, k, l, m, n, o;
struct a p();
q(struct a r) {
  struct a s, t;
  long u = r.b;
  int v = u && w();
  m = n;
  if (v && l) {
    if (e->d.b)
      x();
    e->d = r;
  }
  if (e)
    while (e)
      if (h) {
        t = p(1);
        y(t);
      } else if (k == 0) {
        s = p(1);
        y(s);
      } else
        break;
  if (o)
    r.b = 0;
  while (g) {
    if (r.b && q == f && z() && v)
      r.b = u;
    else
      ;
    if (r.b)
      aa(j);
    else
      i = j;
  }
}
