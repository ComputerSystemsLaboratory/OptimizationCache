b, c, d, e, f, g, h, i;
j() {
  int k;
  while (k) {
    int l, m, a = n();
    if (&b)
      i = n;
    if (i == 0 && e)
      if (g)
        ;
      else if (i == 0 && h)
        if (0 == c)
          ;
        else if (l == d)
          ;
        else if (i && f)
          o();
    k -= m;
  }
}
