struct a {
  int b
} c;
d, e, f, g, h;
i(struct a *j) {
  int a, b;
  if (k()) {
    b = 0;
    for (; b <= 1; b++)
      if (l())
        if (m(j->b))
          n(e, f, g);
  }
  if (k())
    o(j, 487, 127, 0);
  else
    o(j, 487, 127, c);
  if (k())
    o(j, 546, 255, d);
  else {
    p();
    o(j, 546, 255, c);
  }
  for (;;) {
    a = 0;
    for (; a < 4;) {
      q();
      a = h;
    }
  }
}
