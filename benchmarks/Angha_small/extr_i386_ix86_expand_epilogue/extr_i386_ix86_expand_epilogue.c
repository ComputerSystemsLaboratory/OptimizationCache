struct {
  long a
} * b;
c, d, e, f, g, h, i, j, k;
l() {
  int m = !f;
  if (e && g != 2)
    k = 2;
  if (!m || j || f && d && b->a || e)
    n(g == 2);
  if (m)
    if (c)
      o(i);
    else {
      h = 0;
      o(0);
    }
}
