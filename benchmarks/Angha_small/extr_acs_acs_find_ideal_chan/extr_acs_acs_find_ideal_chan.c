struct a {
  double b
};
struct {
  int d;
  long e
} f, *h;
*g;
i;
k() {
  struct a *b;
  long double a;
  int j, l = 1;
  if (h->e && h->d)
    return 0;
  if (h->e && h)
    l = 2;
  for (;;) {
    double m, n;
    int c;
    b = g;
    j = 1;
    for (; j < l; j++) {
      o();
      if (i)
        break;
      a += m++;
    }
    if (j != l)
      continue;
    a /= m;
    if (c) {
      a *= n;
      p(f, b->b);
    }
    if (q() && a < 0)
      p();
  }
}
