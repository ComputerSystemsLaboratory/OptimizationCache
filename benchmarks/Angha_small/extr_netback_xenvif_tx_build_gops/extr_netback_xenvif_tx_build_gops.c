struct a {
  long b
};
c, d, e, f, g, h, i, j;
long k;
l() {
  while (g) {
    struct a a[f];
    int m;
    if (k > &h && n())
      break;
    i = k;
    if (j) {
      m = o(a, m);
      continue;
    }
    if (a[1].b) {
      struct a *b;
      switch (b->b)
      case 9:
        c;
      d = e;
      if (e)
        p();
    }
    if (k)
      q();
  }
}
