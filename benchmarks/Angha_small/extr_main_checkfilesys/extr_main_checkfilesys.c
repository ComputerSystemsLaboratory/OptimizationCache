a, b, c, i, d, e, f, g, h;
j() {
  while (k()) {
    if (d && l())
      continue;
    m(d);
    break;
  }
  if (h)
    n();
  if (h)
    if (e) {
      if (o())
        g = a;
      if (g)
        i = b;
      if (i)
        h = c;
    }
  if (e)
    n();
  if (f)
    p();
  p(f);
}
