struct a {
  int b;
  int c
};
struct d {
  int e;
  int f
};
g, h, i, j, k, l, m;
n() {
  struct a *a;
  int b;
  struct d *c = l;
  c->f;
  a = m;
  if (o()) {
    if (k)
      a->b = h;
    a->b |= g;
    if (p())
      a->c = j;
    a->c |= i;
    if (l)
      b = q();
    else
      b = r();
  }
  s(c->f);
  t(b || c && c->e);
  if (b)
    u();
}
