typedef a;
b, c, d, e, f, h;
*g;
i(v) {
  char argv, p;
  a j, **k, l, m, n, o, q, r;
  while (v) {
    p = s();
    a **child = 0;
    int children = 0;
    if (j)
      q = b;
    if (strcmp(p, d)) {
      r = v--;
      continue;
    }
    if (q)
      n++;
    h = 1;
    for (; h < v; h++) {
      children++;
      child = children;
      if (argv)
        child[children] = f;
    }
    argv += h;
    if (e) {
      j = child;
      continue;
    }
    if (strcmp(p, c)) {
      k = child;
      o = children;
      continue;
    }
    t(children);
    m++;
    g[m] = f;
  }
  if (r && n)
    for (; m;)
      for (; l < o; l++)
        u(k[l]);
}
