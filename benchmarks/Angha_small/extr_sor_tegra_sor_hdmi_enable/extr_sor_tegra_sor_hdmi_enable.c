struct b {
  int c;
  int *e
};
struct {
  int aa;
  int f
} * g;
h, j, k, l, m, n, o, p, q, a, r;
s() {
  int b = t(), i, d = o;
  struct b *c = u();
  if (a) {
    v("");
    d |= m;
  } else {
    v("setting 5.4 GHz link speed\n");
    d |= n;
  }
  w(d);
  d = 0;
  for (; i < 5; i++)
    d |= x(i, c->e[i]) | y();
  if (b) {
    q = 1 + g->f - p + (g->aa - g->f) - 10;
    d = q + 8;
  }
  switch (r) {
  case 6:
    d |= k;
    break;
  case 8:
    d |= l;
    break;
  case 10:
    d |= h;
    break;
  case 2:
    d |= j;
    break;
  default:
    z();
    d |= l;
  }
  ab(d);
  if (c > t)
    ac(1);
  else
    ac(c->c);
}
