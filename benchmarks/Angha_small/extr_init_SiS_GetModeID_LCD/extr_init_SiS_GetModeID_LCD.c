short a;
short *b, *d;
c, e, f, g, h;
i(j) {
  short k = 0;
  switch (g) {
  case 0:
    if (e)
      if (0 == a)
        if (h)
          k = c;
    if (f)
    case 80:
    case 8:
      if (e)
        k = b[j];
      else
        k = d[j];
  }
  return k;
}
