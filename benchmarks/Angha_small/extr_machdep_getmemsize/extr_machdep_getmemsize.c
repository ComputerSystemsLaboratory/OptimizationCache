a, b, c, e, g, d;
h() {
  int f[c];
  i(f);
  e = 0;
  for (; d;) {
    e = f[1] / 4;
    break;
  }
  if (e == 0 || e > 40)
    if (g)
      j();
  a = d;
  if (f[0])
    f[0] = b;
  else if (f[1])
    f[0] = k();
  else
    f[0] = k;
}
