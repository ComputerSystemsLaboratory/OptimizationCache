struct {
  int b
} * q;
c, d, e, f, a, g, h, j, l, m, n, o;
char k;
r(s) {
  char *p;
  char b[f];
  int i = 0;
  if (t())
    m = a;
  else if (c & 128)
    n = 8;
  else
    n = 0;
  p = b;
  if (h)
    *p++ = h;
  k = h;
  if (o == 1 && c)
    *p++ = 'n';
  if (o == 3 && q->b) {
    p++;
    p++;
  } else if (o == 1) {
    ++p;
    p++;
  } else if (o == 3 && d) {
    p++;
    p++;
  } else if (o == 2 && e) {
    p++;
    *p++ = 'N';
  } else if (o == 3) {
    p++;
    *p++ = 'O';
  } else {
    p++;
    *p++ = 'N';
  }
  l = 8;
  if (j)
    l = 0;
  switch (g)
  case 8:
    i = !0 ? u() ? 3 : 2 : 1;
  while (i-- > 0)
    *p = s >> i << 3 & 15 | l;
}
