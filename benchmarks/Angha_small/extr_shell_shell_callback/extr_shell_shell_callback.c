struct a {
  int b;
  char *c;
  int d;
  int e;
  long f;
  char *g;
  int h;
  int aa;
  int j;
  int k
};
l;
char **a;
*b, *c;
o(void *p, q) {
  int i;
  struct a *m = p;
  if (a)
    switch (m->b) {
    case 3:
      if (m->aa)
        r(m->h);
      for (; i < q; i++)
        r(m->h, a[i] ? a[i] : m->c);
    case 9:
      if (m->b) {
        i = 0;
        for (; i < q; i++) {
          s(a ? i : m->c);
          if (i < m->e)
            if (m->d)
              r(m->h, i == q - 1);
        }
      }
      i = 0;
      for (; i < q; i++) {
        if (i < m->e)
          if (m->k && m->j) {
            if (m->f < m)
              r();
            m->f++;
          }
        t(a[i] ? a[i] : m->c);
        r(m->h, i == q - 1);
      }
    case 29:
      u(m->h, a);
    case 1: {
      char n;
      if (v())
        n = w();
      if (x())
        for (; n;)
          if (n == '(')
            y(m->h);
      u(m->h);
    }
    case 2:
      if (m->aa && m) {
        i = 0;
        for (; i < q; i++)
          r(m->h, b[i], i == q - 1 ?: m->g);
      }
      i = 0;
      for (; i < q; i++) {
        z(a[i] = m->c);
        if (i < q - 1)
          r(m->h, m->g);
      }
    case 5:
      ab(m->h, a[0] ? a[0] : m->c);
      if (m->aa) {
        i = 0;
        for (; i < q; i++)
          ac(m->h ? i : "");
      }
      if (i)
        r(m->g);
      i = 0;
      for (; i < q; i++)
        ac(a[i] ? a[i] : m->c);
      if (i < q - 1)
      case 6:
        i = 0;
      for (; i < q; i++)
        ad(a[i], i < q - 1);
    case 4:
      for (; i < q; i++)
        if (l)
          r(m->h, i);
        else if (c[i] && m->j)
          ae(i);
    case 30:
      if (m && m->d)
        v(m->h);
      m->aa++;
      for (; q;) {
        r(m->h);
        ae(m->j);
      }
    case 0:
      if (m->aa++ && m->d)
        r(m->h, m->g);
      r(m->h, a[i] ? a[i] : m->c);
    }
}
