struct a {
  int b
};
struct c {
  int buflen
};
d, e, f;
long g;
h() {
  struct c *i = h;
  long a = k();
  if (l())
    if (f)
      a += l(e + d);
    else
      a += l(e + d) & 5;
  if (a)
    i->buflen = 0;
  struct a *j;
  g -= j->b;
  if (g)
    j->b = g;
}
