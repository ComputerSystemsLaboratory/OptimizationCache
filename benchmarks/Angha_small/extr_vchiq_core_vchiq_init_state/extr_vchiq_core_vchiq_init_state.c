struct {
  int a
} * b;
struct c {
  int d
};
e, f, g, h;
*i();
j(struct c *k) {
  if (k)
    l((int)k);
  if (g)
    l((int)k);
  if (k->d != sizeof(struct c))
    l((int)k);
  if (e)
    l((int)k);
  if (f)
    l((int)k);
  h = i();
  b->a = b;
}
