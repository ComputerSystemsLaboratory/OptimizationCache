a, b, c, d, e, f, g, h, i, j, k;
l() {
  if (d && g)
    m();
  if (d && e)
    m();
  if (d && j)
    m();
  if (d)
    m();
  if (b == a && h && d)
    if (g)
      i = g;
    else
      n();
  if (b != a)
    for (;;)
      if (h && c)
        ;
      else if (h && !c)
        o();
      else
        p();
  for (; f;) {
    if (k)
      q();
    r(f);
  }
}
