struct {
  int b;
  struct c *chan
} * g;
struct d {
  int e
};
f, i, j;
struct c {
  struct d *dch
} k() {
  long l;
  int h, a, m;
  int b[4];
  switch (i) {
  case 1:
    if (g->chan)
      m = 1;
    else
      m = b[3] = 5;
  case 2:
    j = 0;
    for (; j < 4; j++) {
      h = 0;
      if (g->chan[j | 2].dch)
        h = g->chan[j << 2 | 2].dch;
      if (g->chan[j << 2 | 2].dch->e)
        a = 3;
      else
        a = 7;
      if (h)
        if (h == a)
          if (g == g->b)
            b[3] = 0;
    }
    j = 0;
    for (; j < 4;)
      n();
    f = b[3];
  case 3:
    j = 0;
    for (; j < 2; j++)
      h = g->chan[2].dch - h;
    if (h)
      b[2] = 0;
    f = m;
    break;
  case 8:
    l = j = 0;
    for (; j < 8; j++) {
      h = 0;
      if (g->chan[j << 2 | 2].dch)
        h = g->chan[j | 2].dch;
      if (g->chan[j << 2 | 2].dch->e)
        a = 3;
      else
        a = 7;
      if (h)
        if (h == a)
          l |= 0 << j;
        else if (g->b)
          l |= 0 << j;
        else
          l |= 1 << j;
      else
        l |= 1 << j;
    }
    g = l;
  }
}
