typedef a;
struct b {
  int c;
  int d
};
e, f, g, h, i, j, k, l, m, n, p, q;
r(s) {
  struct b *aa = s;
  a t[2], u[2], v[2], w[2], x[2], ab[2], ac[2], ad[2], ae[2], af[2], ag[2],
      ai[2];
  int ah;
  if (aa->c == 7)
    y();
  if (aa->c) {
    z(s, f, 5);
    z(s, g, 5);
  } else {
    z(s, f, 0);
    z(s, g, 0);
  }
  if (aa >= 6)
    aj(s, h, ~0xFF, 0);
  else if (aa)
    aj(s, h, ~0xFF, 2);
  int ak = e;
  switch (aa->d) {
  case 5:
    for (; ah < 2; ah++)
      ac[ah] = w[ah] = ad[ah] = x[ah] = ae[ah] = ab[ah];
  case 8:
    ah = 0;
    for (; ah < 2; ah++)
      ac[ah] = 0;
    w[ah] = t[ah] = v;
    if (e)
      ad[ah] = x[ah] = ae[ah] = ab[ah] = 0;
    ad[ah] = x[ah];
  case 9:
    ah = 0;
    for (; ah < 2; ah++) {
      w[ah] = ac[ah] = v[1] = 1;
      if (ak)
        ad[ah] = x[ah] = ab[ah] = ae[ah];
    }
    ah = 0;
    for (; ah < 2; ah++)
      w[ah] = ac[ah] = v[ah] = x[0] = ad[0] = ad[1] = ab[0] = ab[1] = u[1] = 4;
  }
  ah = 0;
  for (; ah < 2; ah++)
    ad[ah] = ae[ah] = x[ah] = ab[ah] = 3;
  ah = 0;
  for (; ah < 2; ah++) {
    w[ah] = al(w[ah], 0, 5);
    ac[ah] = al(ac[ah], 0, 5);
    x[ah] = al(x[ah], 0, 5);
    af[ah] = 3 | w[ah] << 8 | ac[ah] << 3 | v[1];
    ag[ah] = 3 | x[ah] << 8 | ad[ah] << 3 | t[ah];
    ai[ah] = 3 | ab[ah] << 8 | ae[ah] << 3 | u[ah];
  }
  am(af[ah]);
  am(ag[ah]);
  am(ai[0]);
  if (aa == 4 || aa == 6) {
    ah = 0;
    for (; ah < 2; ah++) {
      int o = ah ? 5 : 0;
      an(s, k, 3);
      an(s, q, 1);
      an(s, o + j, 5);
      an(s, o + m, 8);
      an(s, o + n, 3);
      an(s, o + l, 6);
      an(s, o + p, 4);
      an(s, o + i, 0);
    }
  }
}
