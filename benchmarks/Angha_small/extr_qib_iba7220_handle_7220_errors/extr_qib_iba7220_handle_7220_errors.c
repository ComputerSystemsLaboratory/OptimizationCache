struct a {
  struct b *c
};
struct b {
  int d
};
e, f, g, h, i, j, k, l, m;
n(struct a *p) {
  int o;
  if (q())
    for (; o < i; ++o)
      if (p->c[o].d)
        r(o);
  l = f ? h : g;
  m = e ? j : k;
}
