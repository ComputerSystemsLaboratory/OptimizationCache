struct a {
  int b;
  long aa
};
struct c {
  long d
};
struct e {
  struct a f;
  struct c ab
};
g, h, i, j, k, l, m, n, o, e, f, v, w, x, y, z;
long p, q, r, s, t, u, a, b, c, d;
*ac;
ad(struct e *ae) {
  ae->f.b;
  if (o)
    if (l)
      ae->f.b = v;
    else
      ae->f.b = ac;
  else if (ae)
    if (l)
      if (ae->ab.d)
        ae->f.b = w;
      else
        ae->f.b = x;
    else if (k)
      if (m)
        if (ae->ab.d)
          ae->f.b = e;
        else
          ae->f.b = f;
      else
        ae->f.b = ac;
    else if (g)
      if (ae->ab.d)
        ae->f.b = y;
      else
        ae->f.b = z;
    else if (j)
      if (ae->ab.d)
        ae->f.b = t;
      else
        ae->f.b = u;
    else if (i)
      if (ae->ab.d)
        ae->f.b = c;
      else
        ae->f.b = d;
    else if (h)
      if (ae->ab.d)
        ae->f.b = a;
      else
        ae->f.b = b;
    else
      ae->f.b = ac;
  if (ae->f.b)
    if (ae->f.aa)
      switch (n) {
      case 0:
        if (k)
          ae->f.aa = r;
        else
          ae->f.aa = q;
        break;
      default:
        if (k)
          ae->f.aa = p;
        else
          ae->f.aa = s;
      }
}
