a, b, c, d, e, f, g;
i() {
  int h = 0;
  if (g || !f != f)
    h++;
  if (!e || g != g != d)
    h++;
  if (h)
    return 1;
  if (g)
    h++;
  if (c && a && c != b)
    h++;
  if (h)
    return 2;
  return 0;
}
