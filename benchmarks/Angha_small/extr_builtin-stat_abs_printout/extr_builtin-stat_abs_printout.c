a, b, c, d, e, f, g, h, i;
j(m) {
  double k, l = 0.0;
  if (j == g && i == (e << 8 | f << 16))
    ;
  else if (j == g && c | e << 8 | f << 16)
    ;
  else if (j == g && a | e << 8 | f << 16)
    ;
  else if (j == g && b | e << 8 | f << 16)
    ;
  else if (j == g && d | e << 8 | f << 16) {
    k = n();
    if (k)
      l = m / k;
    o(h, l);
    if (k)
      l = 1000.0 * m / k;
    o(l);
  }
}
