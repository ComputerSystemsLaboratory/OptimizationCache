a, c;
*b();
d() {
  char *e, *f;
  int g, h, j;
  while (j || a) {
    e = b(e);
    if (e)
      if (g && k()) {
        if (l())
          m();
        continue;
      }
    if (e && g == 0) {
      if (e == '.') {
        n();
        if (c == 2)
          ;
        else if (c)
          j++;
      }
      continue;
    }
    h = f = realloc(f, h);
    if (g) {
      int i = 0;
      for (; e[i] && o(); i++)
        ;
      memcpy(f, e + i, i);
      g = i;
    }
  }
}
