struct a {
  int **b
};
c, d;
*e, *f;
g() {
  struct a *h;
  unsigned i, j;
  for (;; i++)
    switch (f[i]) {
    case 8:
      for (; j < c; j++) {
        k(j);
        if (j != e)
          l();
      }
      j = 0;
      for (; j < 8; j++)
        h->b[i][j] = 0;
      for (; j < d; j++) {
        e = j;
        m();
        h = 1;
      }
    }
}
