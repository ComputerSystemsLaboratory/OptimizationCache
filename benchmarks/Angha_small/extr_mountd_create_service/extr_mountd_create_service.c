struct a {
  int b
};
c, d, f, h, j;
*e, *g, *i;
struct a *k;
l() {
  int m = f;
  while (m > 0) {
    --m;
    if (g == 0)
      n();
    g[h] = 1;
    switch (j) {
    case 9:
      o(e[m]);
      break;
    case 8:
      o(e[m]);
    }
    if (e[m]) {
      k = malloc(sizeof(struct a));
      if (k)
        n();
      k->b = c;
    }
    i = malloc(d);
    if (i)
      n();
    getnameinfo(i, d);
  }
}
