struct a {
  int b;
  struct a *c
} * a;
d, e, g, h;
*f;
i() {
  int j = i;
  a = f;
  while (a) {
    if (a->b)
      break;
    a = a->c->c;
  }
  if (a->c->c)
    for (; a->c;)
      ;
  g = 1;
  for (; a->c->c;)
    ;
  while (g)
    if (h && k())
      g = 0;
  if ((j & ~d) == e)
    l();
  if ((j & ~d) == e)
    m();
  if ((j & ~d) == 0)
    if (j & d)
      m();
}
