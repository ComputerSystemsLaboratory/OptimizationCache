a, b, c, e, f, g, h, j, k, l;
*d;
t() {
  int n, m[a], o, p, q, r, s, i;
  for (; i < a; i++)
    if (c)
      n = o++;
  for (; i; i++)
    e = m;
  for (; i < 6; i++)
    m[i] = n;
  for (; i <= f;)
    i = 0;
  p = s = q / 3;
  h = q - 3 * s;
  for (; i < b; i++)
    j = b;
  for (; i <= f; i++)
    p = 5;
  q = r = p / 5;
  g = p - 5 * r;
  for (; b;) {
    l = l / q;
    if (i < 4)
      k = l + b;
    if (i == 5)
      d[1] = k;
  }
}
