struct aa {
  int b;
  int d;
  int e;
  struct f *g
};
struct f {
  int h
};
struct m {
  void *n;
  void *o
};
struct p {
  int q;
  struct m *r
};
struct c {
  int s;
  struct t *u
};
struct {
  void *v;
  void *ab
} * w;
struct t {
  void *ac
};
a, b, c, d, e, x;
*f, *h;
y() {
  int i, j, m;
  struct aa *n = c;
  for (; i < n->b; i++)
    m = n->d;
  for (; i;)
    for (; i < n->b; i++) {
      int o = z();
      struct c *p = p->s;
      j = 0;
      for (; o; j++) {
        int l = j * m;
        if (l)
          p->u[l].ac = 0;
      }
    }
  for (; n->b;) {
    i = 0;
    for (; i < n->e; i++)
      if (n->g[i].h)
        ad(i);
  }
  i = 0;
  for (; i < n->e; i++) {
    struct f *g = n;
    struct p *q = &f[i];
    q->q = i;
    j = 0;
    for (; j < g->h; j++) {
      struct m *r = &r[j];
      ae();
      if (d) {
        r->o = d;
        return b;
      }
      e = q->r;
      x = q->r;
    }
  }
  for (; n->e;) {
    int k = w = &h[k];
    w->ab = a++;
  }
}
