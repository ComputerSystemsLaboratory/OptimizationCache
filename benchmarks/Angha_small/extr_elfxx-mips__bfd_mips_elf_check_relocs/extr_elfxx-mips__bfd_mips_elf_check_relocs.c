struct b {
  int d
};
e, f, i, j, k;
**l;
m() {
  int g, a, b = l = o();
  if (l) {
    struct b *n;
    for (;; n = n->d) {
      int c = n;
      for (; c;)
        break;
    }
  }
  for (;;) {
    int d;
    int *h;
    d = h = l[k];
    switch (d) {
    case 8:
      g = p();
      if (j)
        q();
    }
    if (h)
      f || e;
    switch (d) {
    case 2:
      if (h == 0)
      case 9:
        if (h == 0)
          r(g);
      if (i || h && b) {
        if (a) {
          a = s();
          if (a == 0)
            return e;
        }
        if (i)
          t();
      }
    }
    h &&u();
  }
}
