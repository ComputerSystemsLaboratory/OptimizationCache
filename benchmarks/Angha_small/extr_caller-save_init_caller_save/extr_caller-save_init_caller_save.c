a, b, c;
*d;
e() {
  int f, g;
  for (; g <= b; g++)
    d[g] = g;
  for (; a; f >>= 1) {
    for (; 0 < a;) {
      d[0] && h();
      break;
    }
    if (a)
      c = 1;
    else
      for (; g;)
        ;
  }
}
