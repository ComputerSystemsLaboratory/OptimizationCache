a, b;
c(d, e, f) {
  int g;
  long h = i();
  b &&e;
  if (b && e)
    if (g)
      h;
    else
      j();
  if (b)
    h = 0;
  if (d)
    switch (k()) {
    case 9:
      if (l())
        d = 0;
      break;
    case 8:
      if (m())
        d = 0;
    }
  if (f)
    switch (k())
    case 9:
      if (l())
        f = 0;
  if (d == 0 && f)
    a = h;
}
