*a;
b, c, d;
e() {
  if (f()) {
    e();
    goto g;
  }
g:
  if (c)
    while (f) {
      int h;
      if (c)
        goto g;
      i();
      h = b;
      switch (a[h + 3])
      case 'i':
        continue;
      if (j() || a[h + 3] != 'f')
        if (a[h + 3] == 'f')
          k(d, a + h);
        else
          k(d, a + h, c);
    }
}
