struct b {
  int aa
};
struct ab {
  int c;
  int e
};
f, g, h, a, i, j;
k(l) {
  struct b b;
  struct ab c;
  int d;
  if (f)
    if (m())
      b.aa = 4;
    else
      b.aa = 5;
  c.c;
  while (n())
    switch (j) {
    case 'T':
      if (o())
        c.c = 3;
      else if (o())
        c.c = 2;
      else if (o())
        c.c = g;
      else if (o())
        c.c = 8;
      else if (o())
        c.c = 1;
      else if (o())
        c.c = 9;
      else if (o())
        c.c = 0;
      else
        return 0;
    case 'W':
      p(&c);
    }
  if (b.aa) {
    q(&b);
    for (;;)
      if (r())
        ;
  }
  c.e = 0;
  while (l) {
    s(c.e);
    if (h)
      if (d)
        i;
      else
        t();
    for (;;) {
      while (a)
        ;
      break;
    }
  }
}
