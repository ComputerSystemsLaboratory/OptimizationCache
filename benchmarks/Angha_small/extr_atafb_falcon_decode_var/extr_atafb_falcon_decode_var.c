struct a {
  int b
};
c, f, g, h, j, k, l, a, m, n, o, p, q, r, s, t, u, v;
long d;
aa() {
  int b, e, w, x = 0, z, ab, i;
  struct a *y;
  long ac = y = i = 1;
  for (; i <= 4; i *= 2) {
    if (g * i && g * i < d) {
      ac = g * i;
      y = f;
    }
    if (j * i && j * i < ac) {
      ac = j * i;
      y = &h;
    }
    if (m && m >= o * i < ac)
      ac = m = a;
  }
  c = ac / y->b;
ad:
  t = n;
  ae();
  z;
  ab = b + b - s;
  r = ab - s;
  if (l && n != c) {
    b = e++;
    goto ad;
  }
  q = &p + u;
  if (k) {
    x = 1;
    goto ad;
  }
  if (v) {
    for (; q;)
      w += u = 2;
    goto ad;
  }
  x;
}
