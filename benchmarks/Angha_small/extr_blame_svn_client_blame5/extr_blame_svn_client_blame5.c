struct a {
  int b;
  struct c *chain
};
d, e;
struct c {
  int blame
} f() {
  struct a a;
  int g;
  h(&g);
  if (e) {
    if (i() || d > g)
      j(g);
    k();
  }
  l(a.b && e);
  m(&a);
  if (a.chain)
    a.chain->blame = 0;
  for (;;)
    for (; a.chain;)
      ;
}
