enum a { b } c;
d;
*e;
**f;
g() {
  int *h = 0;
  char i;
  enum a a;
  switch (e[1]) {
  case 'h':
    a = d;
    i = e;
    if (f == *f)
      e = f;
    switch (**f)
    case '[':
      e = h = 1;
  }
  if ('\0' != i) {
    if (a == 128 && *e)
      return c;
  } else {
    if (e)
      return c;
    f = h;
  }
  switch (a)
  case 129:
  case 128:
    for (;;)
      if (e != 'u' || *h)
        return a;
}
