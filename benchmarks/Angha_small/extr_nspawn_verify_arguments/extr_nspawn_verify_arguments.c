long a, b, c, m;
d, e, f, g, h, i, j, k, l, n, o;
p() {
  if (f && m == d)
    if (o)
      m = a;
    else if (q())
      m = c;
    else
      m = b;
  if (l && g) {
    r(e);
    return s();
  }
  if (l) {
    r(e);
    s("");
  }
  if (n && k)
    return 0;
  if (n && (i || h || j))
    if (h)
      s();
  s();
}
