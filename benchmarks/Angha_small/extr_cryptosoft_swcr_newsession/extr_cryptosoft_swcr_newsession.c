struct a {
  int b;
  int *c
};
struct cryptoini {
  int d;
  int e
};
f, g, h, i;
*j(int, int, int);
k(struct cryptoini *m) {
  struct a *l;
  if (m == 0)
    return f;
  for (; m;) {
    switch (m->d) {
    case 8:
      l = j(h, g, l);
      i = m;
      n(l->c);
      j(h, g, l);
    }
    m = m->e;
  }
}
