struct a {
  int b;
  int *c
};
d, e, f, g, h;
j(struct a *l) {
  int k = 0, a, i, b;
  k++;
  a = 0;
  for (; a <= k; a++) {
    m();
    l[a];
    for (; i < 6; i++)
      n();
    if (l->b)
      l->c[a] = d;
    else
      l->c[a] = e;
    if (l->b)
      l->c++;
  }
  b = h;
  if (!(l->b & f) && !(l->b & g))
    b >>= 1;
  o(b);
}
