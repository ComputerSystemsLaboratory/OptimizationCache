typedef long a;
struct {
  long b;
  long(c)
} * d;
e, f;
g() {
  a h = 0, i;
  if (d)
    if (j())
      goto k;
  if (d->b)
    if (l())
      goto k;
  if (d->c) {
    i = 0;
    for (; i < 5; i++)
      h += m(e) + m(f) >> 1;
  }
  if (h)
  k:
    n();
}
