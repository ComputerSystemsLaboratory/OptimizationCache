struct a {
  int b;
  int c;
  int d
};
e, f, g, h, i;
j(struct a *k) {
  int l;
  do
    m();
  while (!(i & g) && !(i & f));
  l = 10000;
  while (l) {
    n();
    l--;
  }
  if (k->b)
    return h;
  if (k->c)
    o();
  p(k->b);
  if (q()) {
    r();
    k->d = k->b;
  }
  k->b |= e;
  if (k)
    ;
}
