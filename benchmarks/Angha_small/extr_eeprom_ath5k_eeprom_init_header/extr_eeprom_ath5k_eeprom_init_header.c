struct a {
  long b;
  int **c;
  int **aa;
  int d;
  long e
};
struct f {
  long g;
  struct a h
};
i, j, k, l, m, n, o, p, q, r, u, v, w, x, e, f, g, h, y, z, ab, ac, ad;
long s, t, a, b, c, d;
ae(struct f *af) {
  int ag, ah, ai, aj = j;
  struct a *ak = &af->h;
  if (af->g)
    return 0;
  al();
  if (ah)
    return e;
  for (; ai; ai++)
    am();
  al();
  if (i) {
    an(aj == j);
    return -e;
  }
  ag = af;
  ao(ag, f);
  if (af->g >= b) {
    ao(l, g);
    ao(m, h);
    ao(n, y);
    if (ak->b >= c)
      ao(o, z);
    if (ak->b >= d) {
      ao(p, ab);
      ao(q, ac);
      ao(r, ad);
    }
  }
  if (af->g < a)
    ak->c[0] = ak->aa[s][0] & al(u);
  ak->c[0] = ak->aa[t][0] & al(k);
  ak = ap(w);
  ak->e = x;
  ak = ap(v);
  ak->d = 0;
}
