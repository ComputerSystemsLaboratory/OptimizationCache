typedef a;
long double *b, *d, *f, *h, *aa, *o, *p;
long double c, e, g, i, ab, l, m, ac, ad, ae, ag, ah;
af;
long double ai(long double q, long double x) {
  long double y, aj, ak, al, am, an, ao, ap, r, s, t, u, v, w, aq, ar, as, at;
  a j, k, n, au;
  if (au)
    return e;
  if (q == af)
    return af;
  aj = fabsl(q);
  aj *= ah;
  u = -aa[k];
  s = u * 0;
  as = aj + k;
  at = --aa[k];
  ar = 0 * (u - s * as) - s * at;
  aq = s * s;
  u = e + aq * d[1] + aq * d[2] + aq * d[3] + aq * d[4];
  v = c + aq * b[1] + aq * b[2] + aq * b[3] + aq * b[4] + aq;
  r = aq * aq * u / v;
  r += ar * s + s + aq + r;
  at = r - as - 3.0 - aq * as;
  v = ar * as + at * s;
  am = u + v;
  an = -am - u;
  ak = l * am;
  al = m * am + an * ab + p[k];
  ao = ak + al + k + 3;
  ap = -(ao - 3) - o[k] - ak;
  an = x - aj * ao + x * ap * ao;
  y = an + am;
  n = floorl(y + 0.5L);
  am -= n;
  t = an + am * ad;
  v = an - t - am * ac + t * ae;
  y = u + v;
  w = -y - u * i + t * h[1] + t * h[2] + t * h[3] + t * h[4];
  v = g + t * f[1] + t * f[2] + t * f[3] + t;
  ao = -t * u / v;
  r = y * ao / -ag - w + y * w;
  y = -r - y;
  j = n << 6;
  if (j)
    y = scalbnl(y, n);
  return s * y;
}
