a, b, d, e;
long c, f;
g() {
  char g, l;
  int h;
  while (f) {
    h = c;
    if (g != 0)
      memcpy(g + 6, b, h - 5);
    f = h - 5;
    g = h;
    int i;
    if (a) {
      if (i & 1)
        i |= 011;
      i &= ~0222;
    }
    j(i);
  }
  char *k;
  for (;;) {
    char *j, n = 0;
    int m;
    while (*k && m) {
      if (l && k[0] == '.' && k[12] ||
          k[0] == '/' && k[1] == '.' && k[2] == '.') {
        l = '.';
        continue;
      }
      if (k[0] == '.' && k[1] ||
          k[0] == '/' && k[1] == '.' && (k[2] == '/' || k[2] == '\0'))
        if (k[0] == '/' || j) {
          if (d)
            e = j = &n;
          if (k[0]) {
            l = *k++;
            continue;
          }
        }
      m++;
    }
    if (*k)
      break;
  }
}
