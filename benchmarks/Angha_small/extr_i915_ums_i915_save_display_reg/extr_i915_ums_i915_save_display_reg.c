struct a {
  void *b;
  void **c;
  void *d;
  void *e;
  void *f;
  void *g;
  void *h;
  void *j
};
struct k {
  int aa
} l;
m, n, o, p, q, r, s, t, u, v, w, a, b, c, d, e, f, g;
void *x();
struct k *y();
z() {
  struct a *h = g;
  int i;
  if (ab()) {
    h->j = x(c);
    h->h = x(d);
    h->g = x(a);
  } else {
    h->j = x(t);
    h->h = x(u);
    h->g = x(r);
  }
  if (ab()) {
    h->f = x(e);
    h->e = x(f);
    h->d = x(b);
  } else {
    h->f = x(v);
    h->e = x(w);
    h->d = x(s);
  }
  switch (y()->aa) {
  case 6:
    for (i = 0; i < 16; i++)
      h->c = ac(p + i * 8);
  case 4:
    for (i = 0; i < 16; i++)
      h->c = ac(o + i * 8);
  case 3:
    for (i = 0; i < 8; i++)
      h->c[i] = x(n + i * 4);
  case 2:
    for (i = 0; i < 8; i++)
      h->c[i] = x(m + i * 4);
  }
  if (ab())
    h->b = x(q);
  else
    h->b = x(l);
}
