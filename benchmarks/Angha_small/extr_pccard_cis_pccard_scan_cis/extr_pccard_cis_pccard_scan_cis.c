struct a {
  int b;
  int c
};
d;
e(*);
*f();
g() {
  struct a h;
  int j;
  struct {
    int common;
    int addr
  } k[5];
  while (1) {
    if (h.b * j) {
      j++;
      continue;
    }
    if (d)
      break;
    switch (h.c) {
    case 8:;
      int i, l = m();
      i = 0;
      for (; i < l; i++) {
        k[i].common = i;
        k[i].addr = f(&h, i + 1);
        e(k[i].addr);
      }
    }
  }
  n(k);
}
