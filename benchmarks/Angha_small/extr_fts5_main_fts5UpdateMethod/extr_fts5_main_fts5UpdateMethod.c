struct a {
  long b
};
struct c {
  int d;
  int base
};
struct e {
  struct c f;
  int g
};
h, i, j;
k(struct a *l) {
  struct e *m = l;
  m;
  int n = m;
  m = m;
  m;
  if (i && o() != i) {
    m;
    m;
  } else
    0 == h &&p(m);
  m;
  m;
  if (0 != h && i)
    ;
  else if (h) {
    m;
    q(&n, m);
  } else {
    m;
    m;
    m;
    r(m->g);
    if (j)
      n = s(m);
    if (n == j)
      n = (m, k);
  }
  s(m->g);
  m;
}
