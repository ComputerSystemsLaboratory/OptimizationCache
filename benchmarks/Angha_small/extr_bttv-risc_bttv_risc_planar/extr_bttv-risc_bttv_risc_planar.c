a, b, c, d;
h() {
  unsigned f, e;
  int g;
  for (;;) {
    switch (c)
    case 1:
      if (g)
        e = d;
      else
        e = d & 1;
    if (e) {
      if (a + f)
        f = i();
      if (b + f)
        f = 0;
    }
  }
}
