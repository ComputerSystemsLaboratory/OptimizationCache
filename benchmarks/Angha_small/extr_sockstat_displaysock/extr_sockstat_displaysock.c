struct {
  int a;
  int b
} * c;
d;
e() {
  if (c->a)
    f();
  if (c->a & d)
    f();
  if (c->a | d)
    switch (c->b)
    case 2:
    case 1:
      if (c->b == 1 && f >= 8)
      case 0:
        g();
}
