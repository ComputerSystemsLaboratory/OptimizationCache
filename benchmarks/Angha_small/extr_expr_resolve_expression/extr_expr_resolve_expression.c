struct a {
  long b;
  int c
};
d, e, f;
long g, h;
i(struct a *j) {
  int k = j, a = j->c;
  switch (j->c) {
  case 8:
    if (j == 8)
      break;
  case 526:
    if (l())
      if (h == d)
        k = a = 30;
    if (g == d == d && (a || a) == h)
      if (g == d && e == 0 || f == 0) {
        if (a == 9)
          ;
        else if (a == 141 || a == 31)
          if (g != d || e)
            a = 30;
      } else if (a == 34)
        ;
      else if (a)
        return 0;
  }
  if (a == 30)
    a = 133;
  j->c = a;
  if (a == 133)
    k += e;
  j->b = k;
}
