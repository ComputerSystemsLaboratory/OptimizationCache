struct a {
  int d
};
struct {
  int c
} * e;
f, g, h, j, k;
l() {
  struct a *a = g;
  switch (j)
  case 8: {
    unsigned i;
    int m;
    for (; i < f; i++) {
      int c = h ? h : e[i].c, b = e;
      if (c && a->d)
        m = i;
      n(b);
      m |= i;
    }
    return m;
  }
    int c = h ? h : e[k].c;
  o();
  if (c)
    n();
}
