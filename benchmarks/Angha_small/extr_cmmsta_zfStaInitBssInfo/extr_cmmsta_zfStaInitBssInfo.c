struct a {
  int *b;
  int *aa;
  int *c
};
struct d {
  int ab;
  int e;
  int f;
  int *b;
  int *aa;
  int *g
};
struct h {
  long j
};
struct {
  struct h ac
} k, *a;
l, b, c;
*m();
n(struct a *o) {
  struct d *d;
  int i, e;
  d = &k;
  for (; i < 6; i++)
    d->g[i] = o->c[i];
  i = 0;
  for (; i < 6; i++)
    d->aa[i] = o->aa[i];
  i = 0;
  for (; i < 8; i++)
    d->b[i] = o->b[i];
  if (a->ac.j)
    p();
  a->ac.j = 0;
  if (c) {
    e = q(&b, &l, c + 8) & 128;
    d->f = 1 | e;
  } else {
    r();
    e = q(&b, &l, c + 8) & 128;
    d->f = 1 | e;
  }
  d = d->e = m();
  d->e = m;
}
