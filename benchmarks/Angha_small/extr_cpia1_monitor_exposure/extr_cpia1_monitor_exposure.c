struct a {
  long b;
  int c
};
struct d {
  int *e
};
long f;
g, h, i, j, b;
k(struct d *l) {
  struct a *a = l;
  int c;
  if (c) {
    if (a == g)
      g = 1;
  } else if (i)
    if (b <= j)
      if (a->b)
        ++a->c;
      else {
        a->b = h;
        a->c = 1;
      }
    else if (a == f)
      ++a->c;
    else {
      a->b = f;
      if (h)
        ++a->c;
    }
}
