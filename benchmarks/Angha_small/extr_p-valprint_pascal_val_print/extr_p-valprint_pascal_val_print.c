a, b, c, d, e, f, h;
*g;
*i();
j(int, *, int);
k(m) {
  switch (n()) {
  case 2: {
    int l;
    for (; (&d)[l] && l < a; l++)
      ;
    h = l;
  }
    if ((m == 0 || m == 's') && g) {
      j(g, i, e);
      f = i();
    }
    if (c && b)
      ;
  }
}
