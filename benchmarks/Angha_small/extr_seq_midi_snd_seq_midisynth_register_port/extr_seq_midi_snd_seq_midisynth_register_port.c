struct a {
  int b
};
struct c {
  int d;
  void *e
};
f, g, h, i, j, l, m;
*k;
n() {
  struct a *o;
  struct c *a;
  int p, b, q;
  a = r();
  q = g;
  b = r;
  for (; b; p++) {
    l = k[p];
    s(p);
    if (p < q)
      ;
    else
      a->e = g;
    if (a->d & f && p < q)
      o->b = j;
    if (a->d && p < m)
      o->b = i;
    if (o->b == a->d)
      o = h;
  }
  if (k)
    for (; b;)
      t(k);
}
