struct a {
  int aa;
  int b
};
c, d, e, a, h, j;
char i;
n(*, char);
o();
p() {
  int b, f, m = 0;
  struct a l;
  char *g = 0;
  char k = h == '#';
  if (n(i, k) || i == k) {
    g = o;
    m = q();
    if (m == 0)
      goto ai;
  } else {
    f = r();
    if (s())
      goto ai;
  }
  if (t())
    l.b = d;
  else
    l.b = c;
  l.aa = j ? 1 : (b = &l) != 0;
  if (j && a == e)
    ;
  else if (a)
    if (d)
      b = 0;
ai:
  if (g)
    u(g);
  if (m)
    v();
  if (f)
    w(b);
}
