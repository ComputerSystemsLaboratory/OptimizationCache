struct a {
  long *b;
  long c;
  long d
};
struct {
  long e
} * f;
g, h, j;
**k, **l;
*m;
n() {
  struct a *o = n;
  long b[2];
  int c, i, p = q();
  int r[3] = {}, a;
  if (p)
    a = 2;
  else
    a = 1;
  for (; h; i++)
    if (m[i])
      break;
  for (; g;) {
    i = 0;
    for (; i < a; i++)
      o = i;
  }
  if (f->e) {
    i = 0;
    for (; i < a; i++)
      b[0] = o = o->c;
    i = 0;
    for (; i < a; i++)
      b[i] = o->b[i];
    c = o;
  }
  i = 0;
  for (; i < a; i++)
    if (r[i] <= 6) {
      if (f->e)
        b[i]--;
      else if (o > 5)
        b[i]++;
    } else if (r[i] <= f)
      if (o < 5)
        b[i]--;
      else
        b[i] -= 2;
    else if (r[i] >= 32 > f->e)
      b[i]--;
  if (r[i])
    if (o < 5)
      c -= 2;
  if (r[i] >= 7 && r[i] <= 32 && f)
    c--;
  if (r[i] && r[i] <= 8 && o > 5)
    c--;
  i = 0;
  for (; i < a; i++)
    if (i > j)
      b[i] = 1;
    else if (b[i] < 6)
      b[i] = 6;
  if (o->d) {
    s(o, 2594, l[c][0]);
    s(o, 2595, l[c][1]);
    s(o, 2596, l[c][2]);
    s(o, 2597, l[c][3]);
    s(o, 2598, l[c][4]);
    s(o, 2599, l[c][5]);
    s(o, 2600, l[c][6]);
    s(o, 2601, l[c][7]);
  } else {
    s(o, 2594, k[c][0]);
    s(o, 2595, k[c][1]);
    s(o, 2596, k[c][2]);
    s(o, 2597, k[c][3]);
    s(o, 2598, k[c][4]);
    s(o, 2599, k[c][5]);
    s(o, 2600, k[c][6]);
    s(o, 2601, k[c][7]);
  }
  m[b[1]] = 5;
}
