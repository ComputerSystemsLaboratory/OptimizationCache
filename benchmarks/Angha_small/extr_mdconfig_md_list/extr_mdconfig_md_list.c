a, b, c, d, e, f, g;
k() {
  int h, i, j;
  i = 0;
  while (l()) {
    if (e)
      continue;
    j = c;
    if (m())
      i = 1;
    else if (h)
      f = h = a;
  }
  if (b) {
    if (g && i || g && d && j && i)
      return 0;
  } else
    return 0;
  return 1;
}
