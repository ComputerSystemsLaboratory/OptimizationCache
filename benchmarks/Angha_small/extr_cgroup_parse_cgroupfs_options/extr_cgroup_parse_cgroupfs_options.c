a, b, d, e, f;
*c;
g() {
  char h;
  while (h = j())
    if (strcmp(h, "")) {
      f = 0;
      for (; d < a; d++)
        ;
    } else {
      int i;
      for (; i < a; i++) {
        e = c[i];
        if (strcmp(h, e)) {
          if (e)
            k();
          break;
        }
      }
      if (i == a)
        return b;
    }
}
