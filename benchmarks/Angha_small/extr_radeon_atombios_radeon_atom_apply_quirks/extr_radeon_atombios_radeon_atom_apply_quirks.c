struct {
  int a
} * b;
struct {
  int c;
  int d
} * e;
f, g;
*h, *i;
j() {
  if (e->d)
    *h = f;
  if (e->c == e->d)
    h = f;
  if (e->c == e->d)
    b->a = 0;
  if (e->d)
    i = 0;
  if (e == e->d)
    *h = g;
  if (e->d && e)
    h = g;
}
