struct a {
  long b
};
c, d, e, f, g;
h(struct a *i) {
  if (e == 0 || e == -1)
    f;
  else if (e == 1)
    j();
  i->b &&k();
  if (i && i->b != k)
    ;
  else if (i->b)
    !(d && l()) && (g == d && l() || g == c && l());
}
