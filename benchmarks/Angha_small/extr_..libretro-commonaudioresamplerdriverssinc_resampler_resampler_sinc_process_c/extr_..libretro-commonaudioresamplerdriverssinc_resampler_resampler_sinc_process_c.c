struct a {
  int b;
  int c;
  int d;
  long e;
  long f;
  float *g;
  float *h;
  int i;
  float j
};
k, l, m, n;
o(void *p) {
  struct a *q = p;
  float *a, *b = l, *s, *g = q->c;
  while (l)
    q->g[q->f] = q->g[q->e] = *a++;
  q->h[q->f] = q->h[q->e] = *a;
  float r;
  s = &s;
  n = (q->d & q->i) * q->j;
  if (q == k)
    r = g[m] * s[m];
  b[0] = r;
}
