a, b, c, d, f;
*e;
k() {
  int g, h, i, j;
  f = 0;
  if (a)
    for (;;) {
      switch (c)
      case 9:
        i = f;
      if (j % 2)
        continue;
      j++;
      if (b) {
        g = 0;
        continue;
      }
      if (d)
        continue;
      if (i > g)
        g = h = e[h];
    }
}
