typedef long a

;
int b;
a c;
int d_e;
a * d_f;
void d( void) {
  static g, h, bmin;
  
  a **ak  ;
  
    if (h == 0) 
      for (; ak ;) {
        b = d_f[*ak++[d_e]];
        if (b ) {
          if (b < bmin)
            bmin = b;
          h++;
        
      }
    }
    
      
      for (; ;);
    
  
}
