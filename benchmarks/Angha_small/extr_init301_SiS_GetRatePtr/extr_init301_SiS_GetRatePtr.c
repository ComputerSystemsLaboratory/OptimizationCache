struct {
  short a;
  short b
} * c;
struct d {
  short e;
  short f
} g(*, short, short, short, *), *j;
h, k, l, m, n, o;
short p() {
  short q, i;
  j[m];
  if (k)
    if (j[m].e)
      return 0;
  if (l < 4)
    return 5;
  q = j[m].f;
  if (o)
    q++;
  do
    if (c[q + i].a)
      i++;
  while (n);
  if (c[q + 1].b)
    g(h, l, m, q, &i);
  return q + i;
}
