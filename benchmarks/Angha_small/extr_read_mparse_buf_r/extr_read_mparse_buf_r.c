struct {
  struct b *c
} * o;
struct buf {
  struct buf *d
};
struct b {
  int e
};
f, g, h, k, l, m, n;
*j;
p() {
  long i;
  struct buf *a;
  int q;
  while (i < j) {
    if (k) {
      o &&r(&i);
      j[i++];
    }
    if (j)
    rerun:
      q = a = o;
    switch (h) {
    case 4:
      if (o->c->e)
        s();
      o->c ?: t(o);
    }
    if (a && q & g)
      a = a->d;
    if (a) {
      if (q & f)
        m = '\0';
      if (l < a)
        u();
      v(l);
      goto rerun;
    }
    n = q & f ? w('\0') : 0;
  }
}
