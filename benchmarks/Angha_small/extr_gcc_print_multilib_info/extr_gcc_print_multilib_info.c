a, b, c;
g() {
  char *f = g;
  while (*f)
    while (*f != ' ') {
      char *e = b, *h;
      while (*e)
        while (e)
          if (*e)
            i();
      while (h != ';') {
        char d;
        if (*h)
          d = h;
        if (d)
          ++h;
      }
      *h = c;
      for (; *h != '\0';) {
        if (a)
          j();
        j(*h);
      }
    }
}
