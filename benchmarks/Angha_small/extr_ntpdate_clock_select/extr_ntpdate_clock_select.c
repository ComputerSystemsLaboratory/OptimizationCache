a, b;
long c;
d() {
  long e;
  int *f[a];
  int *g;
  for (; c; c--)
    f[c] = f;
  for (; e < b; e++)
    if (0 == b)
      ;
    else if (1 == b)
      g = f[0];
    else {
      for (; e;)
        ;
      g = f[0];
    }
  return g;
}
